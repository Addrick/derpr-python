---
name: Codebase architecture detail
description: Full structural reference — components, data flows, schemas, interfaces, config, and key implementation details
type: reference
---

## Core Data Flow

```
Interface (Discord/Gmail/Zammad)
  -> Discord bot: preprocess_message() for dev commands (sent to threads, not logged)
  -> ChatSystem.generate_response()
    -> BotLogic.preprocess_message()  -- command detection (set, what, hello, etc.)
    -> RequestBuilder.prepare_request()  -- resolve services, build history, filter tools, append user msg
      -> build_conversation_history()   -- fetch sliding window from DB
      -> retrieve_memory_block()        -- embed (sliding window + current msg), KNN search,
                                           inject <memory> block as user msg at position 0
                                           (skips memories already in sliding window via
                                            exclude_after_interaction_id; respects include_ambient)
    -> ToolLoop.run()                 -- LLM call + tool execution loop
                                         (budget: MAX_TOOL_CALLS = 15 tool calls,
                                          guard: MAX_TOOL_ITERATIONS = 25 round trips)
      -> TextEngine.stream_messages()   -- provider-specific streaming API call
      -> ToolManager.execute_tool()     -- tool execution
      -- yields TokenEvent / ToolCallStartEvent / ToolCallResultEvent / ErrorEvent
      -- + ToolDeferredEvent per gated write (mid-turn, DP-297 — the loop keeps
         running, so one turn can park several and still end on ordinary text)
      -- + internal _ApiPayloadEvent (orchestrator caches), _ToolContextEvent
         (sealed tool span on the error paths), _WriteDuplicateEvent, and
         _LoopFinishedEvent (orchestrator persists the assistant turn)
    -> MemoryManager.log_message()    -- persist user + assistant messages (reply_to_id links A->Q)
    -> Response back to interface

Background (SqliteConsolidator, agent_name="memory", every 15min — registered ONLY when SEMANTIC_BACKEND=="sqlite"; Hindsight backend obsoletes it):
  Phase 1 — Embed unembedded messages (loop until exhausted per channel):
  -> get_active_channels()            -- find channels with unprocessed msgs
  -> get_unembedded_messages()        -- fetch msgs not yet in Message_Embeddings
  -> EmbeddingService.encode()        -- token-aware batch chunking
  -> INSERT INTO Message_Embeddings + vec_Message_Embeddings

  Phase 2 — Segment and summarize (loop until clear per channel):
  -> get_unsegmented_embedded_messages()  -- msgs with parent_summary_id IS NULL
  -> _segment_by_similarity()         -- centroid-based topic grouping (threshold 0.80)
                                         bridging: explicit reply_to_id link OR
                                         heuristic (lone user msg + next assistant msg)
  -> _summarize_segment()             -- LLM extracts observations via submit_memory_summary tool
  -> Store segment + summary + vec embedding in Memory_Segments / Memory_Summaries / vec_Memory_Summaries
  -> SET parent_summary_id on processed msgs

Background (MemoryConsolidator, hourly daemon via app.register_task):
  -> cluster similar episodic summaries (L1) by pairwise similarity
  -> LLM merges clusters into core profiles (L2) via submit_core_profile tool
```

## Key Components

### `src/chat_system.py` -- ChatSystem
- Orchestration kernel + public API only (post DP-200/DP-201b). Constructed by `src/bootstrap.create_chat_system` with real deps: `__init__(memory_manager, text_engine, embedding_service=None, *, personas, system_persona_names, tool_manager, models_available=None)`. Borrows `memory_manager.backend` as `self.memory_backend` and pushes the embedding service into it via `set_embedding_service` so `SqliteSemanticBackend.recall` can translate query → embedding.
- Collaborators (DP-200 slice B; DP-201b deleted the transitional private delegate seams — internal and external callers address these directly):
  - `self.request_builder` (`src/request_builder.py` — RequestBuilder): `prepare_request()`, `build_conversation_history()`, `retrieve_memory_block()`, `format_raw_history_for_llm()`, `filter_tools_for_persona()`, `format_memory_block()`, sticky taint map (`conversation_taints` / `set_conversation_taint()`), `resolve_generation_params()`, dry-run `assemble_request()`.
  - `self.turn_persistence` (`src/turn_persistence.py` — TurnPersistence): `log_user_turn()`, `commit_or_update_assistant()`, `retain_turn_safe()`, `store_api_request()` + the `last_api_requests`/`last_api_iterations` caches (DP-202 deleted the mirroring ChatSystem properties; BotLogic receives TurnPersistence as an explicit dep for dump_last/dump_history).
  - `self.confirmations` (`src/confirmations.py` — ConfirmationManager): `pending` park map keyed by **token** with a per-conversation index `_by_key` (DP-297 — keying on `(user, persona)` held at most one park, so a second write evicted the first); `park()` / `take()` / `restore()` / `list_for()`, the decision queue `enqueue()` / `drain()`, `apply()` (execute-then-patch), `patch_parked_entry()`, lazy expiry `sweep_expired()` / `expire()` / `is_expired()`, and `lock_for()` (one asyncio.Lock per conversation, serializing execute → patch → continue). The `ParkedWrite` and `Decision` dataclasses live here; `PendingConfirmation` is gone. **DP-319 — durable.** Every mutation writes through to `Parked_Writes` (own table, NOT `Proposals`: that executor's `action_type` whitelist is the privilege line, and a chat park is an arbitrary tool call). Status vocabulary `PARK_DB_*` in `memory_manager`: live = `pending` / `claimed`; terminal = `resolved` / `expired` / `interrupted` / `quarantined`. `rebuild_from_store()` runs once from `create_chat_system` and reconciles each row — restore, expire-properly, terminate a `claimed` row as `interrupted_by_restart` (**never re-executed**), or quarantine one that cannot be read or reconciled (its payload is erased and an `audit_park_quarantined` row is written). Terminal rows survive for `PARK_ROW_RETENTION` so `already_resolved()` can recognize a re-proposal of an action that already ran; a park whose row was refused keeps that guard in an in-process fallback instead. `get_parked_write_status` is tri-state (`status` / `None` / `PARK_DB_UNKNOWN`) — `restore()` fails closed on the third, or a locked store resurrects a decided write as approvable.
- `generate_response()` -> returns `Tuple[str, ResponseType, Optional[int], Optional[int]]` (collect-stream wrapper over the kernel)
- `_orchestrate()` -> shared streaming kernel — preprocess (**which is also the DP-330 origin gate**) → set TurnContext (`src/tools/turn_context.py`) → `request_builder.prepare_request` → user-log → user `retain_turn` → ToolLoop → assistant-commit → assistant `retain_turn` (taint-aware) → DoneEvent → reset TurnContext. `stream_response()` (portal) and `generate_response()` (Discord/Gmail/agents) both delegate here. Token + tool events forwarded as-is; internal `_ApiPayloadEvent` siphons into `turn_persistence.store_api_request`, `_LoopFinishedEvent` drives universal write-audit parking + assistant persistence. Both retain calls are fire-and-forget (sqlite_legacy noop; Hindsight enqueues + returns).
- **Persona origin allowlist gate (DP-330)** — enforced in **`BotLogic.preprocess_message`**, not in this kernel. First thing that function does, above command parsing: `Persona.is_addressable_from(origin)`; a persona whose `origin_allowlist` is non-empty and does not match gets `Persona '<name>' is not available from this channel.` back as an ordinary command result (the message deliberately discloses nothing about the allowlist), which every caller already renders and returns on. Three placement facts are load-bearing:
  - **(a) At the chokepoint, not in an adapter.** `preprocess_message` is the one seam every message-bearing surface passes through. The first cut put the gate in `_orchestrate` and that was **wrong**: `discord_bot.on_message` and the portal's `/api/v1/persona/{name}/dev_command` both call `preprocess_message` directly and **return before the kernel is entered**, so on the only transport the allowlist can ever match, `what prompt` / `detail` / `dump_history` — and `what origin_allowlist`, which prints the very ids the refusal withholds — all answered in full. Any future adapter that short-circuits dev commands inherits the gate for free.
  - **(b) Above command dispatch**, unlike the DP-128 quarantine gate: quarantine deliberately lets `set tools` through so the operator can repair the persona live, whereas an origin that may not address the persona must not read its config with `what prompt` either. Above *parsing* too, so a plain chat turn — which reaches no handler — is refused as well.
  - **(c) Continuation turns are exempt, structurally.** The kernel skips preprocessing entirely when resuming a resolved park, so the exemption falls out of where the gate lives rather than being a special case that can drift. Intended: the approved write has already executed by then, so refusing would suppress the summary of an action that ran. A disallowed origin cannot raise a park in the first place, so it has nothing to resolve. ⚠️ The park *token* is not itself a secret — `GET /api/v1/session/{persona}/transcript` publishes it — but resolving still requires the operator token (portal) or the owning Discord account, so the exemption does not widen approval.
  - ⚠️ **Scope: this gates addressing, not the portal's read seams.** `GET /api/v1/persona/{name}`, `/session/{p}/assemble`, `/session/{p}/transcript` and `/kobold_export` reach `request_builder` without going through `preprocess_message`, and `_setup_control_plane_auth` waves through every GET — so a restricted persona's prompt, assembled request and history remain readable by anyone who can reach the portal. Pre-existing (DP-330 neither caused nor fixed it); closing it is a portal-auth change, not an allowlist change.
- `resolve_park()` / `stream_resolve_park()` -> write-tool approval flow, keyed by a mandatory park **token** (with several writes resolvable per conversation, `(user, persona)` no longer identifies one). All write tools park for audit, not just CONFIRM mode. Sequence: `take()` the park synchronously (so a double-click cannot execute twice) → acquire the conversation lock → `drain()` every decision that arrived while waiting → `ConfirmationManager.apply()` each in approval order → ONE continuation turn over freshly-rebuilt history. The continuation opens with `_render_resolution_nudge` (a synthetic, deliberately unpersisted user turn) and carries `_ContinuationState` into `_orchestrate`; this replaced DP-124's `_ResumeState`, whose replayed *snapshot* of history could not survive a burst of independently-resolvable parks.
- `stream_resolve_deferral(token, kind=, status=, result=)` -> **DP-345**, the twin entry point for kinds whose answer comes from an authority rather than a human. Everything after the claim is literally the same code — both entries funnel into `_stream_settle(decision)`, which owns lock → drain → apply → ONE continuation — because folding, patching and continuing were never approval-specific. Only the **claim** differs, and it has to: a click needs the conversation-ownership, TTL and persona checks; an authority ping needs none of them. Both claim by **token** — see the handle-is-the-token note under `confirmations.py`. `kind` here is what the caller **expects** to be answering, not a lookup key: the claim is `take(token)` and the kind is checked against the row afterwards, and **both directions fail closed** — an `approval` park is refused outright (settling one here would run a human-gated write with no human in the loop), and a mismatch against the caller's expected kind is refused too (its `status`/`result` then describe some other piece of work). Each restores the park rather than dropping it. `status`/`result` are the **re-read** outcome — the trigger carries an identifier, never facts. A ping matching nothing (the retry, an expiry) returns silently: there is no conversation to interrupt about a job the model was already told about.
- **Burst folding comes free across kinds.** A node-job ping landing while an operator is approving something in the same conversation joins that batch via `lock_for((user, persona))` instead of racing a second tool loop over the same history. Neither re-derived wake path could do that — they entered through `generate_response` and never touched this lock. ⚠️ That same entry is the bug the split produced: `_orchestrate` calls `log_user_turn` when `continuation is None`, so **both re-derived paths persisted their whole synthetic wake message as a durable user row** — under the operator's real Discord id for DP-343 — and every later turn in that channel re-read the instruction block as something the operator had typed.
- Public request-assembly delegates kept on ChatSystem (portal/inspector surface): `assemble_request()`, `get_view_history()`, `get_session_memory_block()`
- `RequestContext` dataclass (in `src/request_builder.py`) bundles all pipeline state
- Re-exports for back-compat: `RequestContext`, `AssembledRequest`, the generation-event names, `_relative_time`, `MAX_CONVERSATION_TAINTS`, `MAX_CACHED_API_REQUESTS`.

### `src/bootstrap/` -- composition root (DP-200 B1)
- `load_all_personas()` (user + system personas), `build_tool_manager(memory_manager, personas)` (registers WebSearchHandler, MemoryToolHandler, MemoryRecallHandler, IngestPathHandler), `create_chat_system(memory_manager, text_engine, embedding_service=None)` — signature-mirrors the old constructor so call sites swapped 1:1. It also calls `register_credentials()` (wires the credential vault → egress scrubber; see `src/security/`) and passes `models_available=get_model_list()` into the ChatSystem constructor.
- Also where `confirmations.rebuild_from_store()` is called (DP-319) — deliberately here rather than in `main`, because a rebuild that is never called yields an empty pending set over a table of live proposals, which looks exactly like the feature working.
- Boundary rule (enforced by `tests/test_module_boundaries.py`): only the entrypoint may import `src.bootstrap`. Tests/fixtures/scripts use `create_chat_system`; pure-unit construction can call `ChatSystem(...)` directly with explicit deps.

### `src/request_builder.py` -- RequestBuilder (DP-200 B3)
- Everything between "message arrived" and "wire payload": `build_conversation_history` (per memory mode, via `fetch_raw_history` dispatch), `format_raw_history_for_llm` (incl. tool_context replay + group-chat name prefixes; **DP-296 ordering guard** — a replayed tool block is dropped unless the preceding entry is a `user` turn, because the history limit can leave a tool-bearing assistant row oldest and Gemini rejects a wire array that opens with a function call: *"function call turn must come immediately after a user turn or after a function response turn"*), `retrieve_memory_block` (backend `recall` → `<memory>` block; `MEMORY_RETRIEVAL_ENABLED`/`MEMORY_MAX_SUMMARIES_IN_CONTEXT`; `exclude_after:N` recency tag), `filter_tools_for_persona` (policy + bindings + model compat), `resolve_generation_params`, `prepare_request` (history + LTM injection + client_messages fallback + token budget via `truncate_messages_to_budget`, then `drop_orphaned_tool_head`), `assemble_request` (S5 `/assemble` dry-run — parity with live by construction), `get_view_history` / `get_session_memory_block` (DP-130/136 public seams), module-level `build_scope_tags`.
- Owns the sticky per-conversation taint map (`conversation_taints`, LRU-bounded by `MAX_CONVERSATION_TAINTS`); read during prepare, written back by the kernel post-loop.
- Persona access via `persona_lookup` closure (NOT a dict reference) so post-init `chat_system.personas` rebinds stay visible.

### `src/deferral_kinds.py` -- the deferral vocabulary (DP-345)
- One leaf module holding `DEFERRAL_KIND_APPROVAL`. A **deferral** is one tool call whose real result arrives after the turn that made it ended: the turn completes with a placeholder tool result, a durable row binds the pending call to that turn's own coordinates, and whatever external event answers it patches the placeholder and runs one continuation turn.
- It is a leaf because it has to be. `src.tools.tool_loop` writes the placeholder and `src.memory.memory_manager` renders the kind into the `Parked_Writes` DDL default, and the module-boundary contracts forbid those two from importing each other — so the constant they must agree on byte-for-byte has nowhere else to live that is not a duplicate. `test_module_boundaries.py` pins it as a leaf for that reason.
- **The rule it carries:** *the coordinates of a resume come from the turn, never from config.* A kind that must be told which persona/channel/user to answer in has discarded state it was already handed — which is precisely what `MODEL_JOB_WAKE_PERSONA` / `_CHANNEL` / `_USER` and `CC_FIXR_PERSONA` / `_CHANNEL` are, and why DP-345 deletes them rather than generalizing them.

### `src/confirmations.py` -- ConfirmationManager + ParkedWrite (DP-200 B2, rewritten DP-297 / DP-319, generalized DP-345)
- Orchestrator-owned store for gated writes, keyed by **`token`** with a per-conversation index `_by_key[(user, persona)]`. DP-297 replaced the old 1:1 `(user, persona)` key (where a second park evicted the first, emitting `audit_parked_evicted`); one turn can now queue N proposals, each resolvable independently and out of order. ToolLoop stays stateless across the park. `PendingConfirmation` is gone — `ParkedWrite` and `Decision` are the dataclasses here.
- **DP-345: the store is no longer approval-only.** `ParkedWrite` gained exactly one field, `kind` — which external event answers this call. **⚠️ There is no `handle` field, and re-adding one is the mistake to avoid: the handle IS the token.** derpr mints the park token and hands that same string outward as the job id (`services/pve/derpr-model-install` takes it as `JOB` and validates `^[a-z0-9][a-z0-9-]{0,63}$`, which a 32-char lowercase-hex token satisfies), so the thing the node names in its callback already IS the park. A second identifier, plus an in-memory index to join it back, was state the parking call already had — the exact smell this ticket exists to remove. Every kind therefore claims with plain `take(token)` (sync pop + durable row claim), and `rebuild_from_store` reloads `pending` from the rows — the durability DP-343's in-process `OrderedDict` did not have, so a ping arriving after a restart resolved nothing at all.
- **⚠️ The accepted trade-off of that choice:** the park token now crosses to the node. It is not a secret (`GET /api/v1/session/{persona}/transcript` publishes it) and it cannot be replayed as an approval, because `stream_resolve_park` refuses non-approval kinds and `stream_resolve_deferral` refuses approvals — but one identifier now spans a trust boundary. If tokens ever come to confer authority rather than merely address a row, this is the first thing to revisit.
- **Why `kind` stays when `handle` went.** It is not derivable from the call: `install_model` is an approval park before it runs and a node-job deferral after — same tool, same arguments. Three branches read it after a restart (`list_for` offers a button or not, `stream_resolve_park` accepts a click or refuses, `apply` executes or does not), and each is a fail-closed guard against an operator click re-running an already-executed irreversible call.
- **`list_for` defaults to `kind='approval'`, and that default is fail-closed.** Every caller of it renders an approve/deny affordance (portal pending list, Discord re-post, kobold transcript). A node job has no decision for a human to make, and `apply()` on one would run the deferred call a second time — the tool already ran, which is why there is a job to wait on. A new kind becomes clickable only when a surface asks for it by name. `stream_resolve_park` refuses a non-approval kind for the same reason, and restores it.
- `ParkedWrite` is ONE call, deliberately carrying **no conversation snapshot** — the continuation rebuilds live history from the DB. That is what makes out-of-order resolution safe: every snapshot would predate its siblings, so replaying one forks the conversation.
- **Durable since DP-319**: the `Parked_Writes` table (SQLite). In-memory dicts stay the live index (`take` must remain a pure sync pop to keep its atomicity, and `asyncio.Lock`s cannot be persisted), the DB is **written through** on every mutation, and read back once at boot.
  - Row states (`PARK_DB_*` in `memory_manager`): `pending → claimed → resolved | expired | interrupted | quarantined`. `take()` pops in memory AND `claim_parked_write`s the row; `restore()` releases it. `quarantined` is its own terminal state, not a flavour of `interrupted` — an interrupted park may have executed, a quarantined one provably did not, and `resolution` is the column a forensic query filters on. This vocabulary is deliberately **not** the same as `tool_loop`'s `PARK_STATUS_*` (which is what the *model* reads in replayed history); they overlap by accident on `"expired"` and diverge on purpose, so the near-homonyms must not be used interchangeably.
  - `restore()` is tri-state. `release_parked_write` returning False is ambiguous, so it consults `get_parked_write_status`, which answers `status` / `None` (no such row) / `PARK_DB_UNKNOWN` (the store could not tell). **Fails closed on the third** — a transient `database is locked` read as "no row" re-INSERTs `status='pending'` over a decided row and resurrects an executed irreversible write as approvable.
  - A park whose INSERT was refused (`insert_parked_write` serializes `write_call` strictly, so a `datetime` argument hashes fine but will not store) stays live in memory with `persisted=False`. `apply()` then records its outcome in an in-process fallback map, because the DB-backed re-execution guard cannot see a row that does not exist.
  - `finalize_parked_write` keeps the row (the re-execution guard reads it) but NULLs `write_call` / `audit_info` / `confirmation_text` — a decided park retains only a **hashed** `call_identity` plus its outcome. `purge_parked_writes` drops terminal rows past `PARK_ROW_RETENTION`.
  - `rebuild_from_store()` runs from the composition root (`src/bootstrap`), not from `main`. Three populations: in-TTL `pending` reinstated; past-TTL `pending` properly expired (patch + audit — the lazy sweep only walks what is loaded, so a row it never loaded is one it never expires); `claimed` terminated as `interrupted_by_restart` and **never re-executed**, because the write may already have run. A fourth outcome, `quarantined`, covers a row that cannot be *read* (undecodable `write_call`) or cannot be *reconciled* (anything raising out of the per-row work): forced terminal, payload erased, `audit_park_quarantined` logged. Both branches must quarantine — returning instead leaves the row `pending` forever, since `purge_parked_writes` only deletes terminal rows, so it fails identically on every later boot while its unscrubbed arguments stay on disk.
  - `rebuild_from_store` also marks `_last_purge`; the retention purge otherwise ran twice, once at boot and again on the first `park()`/`list_for()`.
  - Deliberately NOT the `Proposals` table: `ProposalExecutor`'s `action_type` whitelist IS the ADR-2026-07-04 privilege separation, and a chat park is an arbitrary tool call.
- Surface recovery after a restart: the portal is free (`list_for` on every transcript load); Discord's `_confirm_registry` (message id → token) and `_rendered_park_tokens` both die with the process, so old buttons go dead and `_post_pending_proposals` re-posts on the next message in that channel. A click on a dead button is answered (`_notify_stale_button`) rather than dropped — gated on the bot's OWN ✅/❌ still being on the message, which is what distinguishes a proposal from an ordinary answer someone reacted to. ⚠️ **the handler must be `on_raw_reaction_add`**: discord.py dispatches the cooked `on_reaction_add` only for messages still in its `max_messages` (1000) client cache, which is empty after a restart — i.e. it cannot fire in the exact case this exists for, and it also stops resolving live parks whose message ages out.
- Lifecycle surface (the seam): `park` / `take` / `restore` / `list_for` / `lock_for` / `enqueue` / `drain` / `apply` / `patch_parked_entry` / `sweep_expired` / `expire` / `is_expired` / `note_duplicate_ref` / `already_resolved` / `rebuild_from_store`.
- `apply()` settles ONE deferral: it produces the real result, then patches the history entry **in place** — ordering matters, or the continuation reads its own proposal as still pending. **The head branches on kind; everything from the audit row down is shared** (DP-345). An approval executes or refuses; every other kind executes **nothing** — its tool already ran on the turn that deferred it, so running it again would be a second irreversible action reached through the one method whose job is preventing that. Non-approval kinds supply `Decision.outcome_status`, which wins over the `approved`/`ok` derivation because there was no operator verdict to derive from; `apply` raises if it is missing. `Decision.status` separates the two axes (`approved` = operator verdict, `ok` = did the tool run) so approved-but-failed is not recorded as success. `DENIAL_INSTRUCTION` / `INTERRUPTED_INSTRUCTION` live in the patched entry, not in an ephemeral nudge, so the verdict and what to do about it share one lifetime.
- Re-execution guard (DP-319): `already_resolved` answers "did this exact call already RUN?" via `find_resolved_parked_write`, scoped to `PARK_STATUS_APPROVED` inside `PARK_REEXECUTION_GUARD_WINDOW` (15 min). Injected into ToolLoop as `resolved_lookup`, beside `pending_lookup`. Needed because `pending_lookup` is structurally blind during a continuation (the park was already taken) — which is exactly when the model re-proposes. **Continuation turns only**, and that scoping is the correctness argument: left running on ordinary turns it would answer a legitimate "restart that service again" with "that already happened", silently, for fifteen minutes. The identity it keys on is `tool_loop.write_call_identity_hash` (name + canonicalized args, sha256) — deliberately beside `write_call_identity` rather than on `MemoryManager`, since drift between the canonicalizer and the digest fails OPEN as a silently disabled guard.
- Uses a `tool_manager_lookup` **closure**, not a bound reference, so a post-init `chat_system.tool_manager` swap is visible here (ToolLoop reads per-call).

### `src/turn_persistence.py` -- TurnPersistence (DP-200 B4)
- Turn write-paths: `log_user_turn` (fresh user row, or `handle_portal_retry` archival on retry; empty-message phantom-row guard), `commit_or_update_assistant` (UPDATE on retry / INSERT otherwise; returns canonical assistant id), `retain_turn_safe` (fire-and-forget `memory_backend.retain_turn` with scope tags + untrusted bit).
- Owns `last_api_requests` / `last_api_iterations` (dump_history's data; LRU-bounded by `MAX_CACHED_API_REQUESTS`) via `store_api_request`. ChatSystem exposes both as read/write properties; BotLogic receives TurnPersistence as an explicit dep (DP-202).
- One of the egress seams where the `SecretScrubber` is enforced (API-payload caching).

### `src/generation_events.py` -- streaming event dataclasses
- Lives outside chat_system to break a circular import (chat_system ↔ tool_loop). chat_system re-exports the names so `from src.chat_system import TokenEvent, DoneEvent, ResponseType` still works.
- `TokenEvent(delta)` -- incremental text chunk
- `DoneEvent(text, response_type, assistant_id, user_interaction_id)` -- terminal success
- `ErrorEvent(message)` -- terminal failure
- `ToolCallStartEvent(tool_name, arguments, call_id)` -- tool invocation surfaced mid-stream (tool revamp v1)
- `ToolCallResultEvent(call_id, tool_name, result, error)` -- paired result; `error` populated on tool failure
- `PendingConfirmationEvent(text, write_calls, persona_name, token, audit_info=None)` -- one gated write surfaced mid-stream as its own approve/deny affordance. ⚠️ Do not confuse it with `tool_loop.ToolDeferredEvent(token, write_call, audit_info, confirmation_text, turn_tainted, kind)` (named `WriteParkedEvent` before DP-345), which is the *loop*-level event: ToolLoop emits a `ToolDeferredEvent`, and `_orchestrate` translates each one into a public `PendingConfirmationEvent` (with `write_calls=[ev.write_call]`) for interfaces. `ToolDeferredEvent` is public-named but lives in `tool_loop`, not here. **Since DP-345 the translation is conditional on `kind == 'approval'`** — a deferral with no human question in it must not surface an approve/deny affordance.
- `GenerationEvent` Union; `ResponseType` enum: DEV_COMMAND, LLM_GENERATION, PENDING_CONFIRMATION. **The `PENDING_CONFIRMATION` *enum member* is vestigial as of DP-297** — nothing sets it as a turn's response type any more (a turn that parks writes still ends with real text and persists as LLM_GENERATION). It survives only so old rows and the retry branch in `turn_persistence` keep their meaning. The *event class* `PendingConfirmationEvent` is very much alive and is still yielded per parked write.
- `last_api_requests` dict caches payloads for dump_last/dump_history (keyed by user+persona)

### `src/tools/tool_loop.py` -- ToolLoop
- Stream-shaped tool loop extracted from `_orchestrate` (tool revamp v1, supersedes the old `plans/toolloop_extraction.md`).
- `ToolLoop(text_engine, tool_manager, max_iterations=MAX_TOOL_ITERATIONS, max_tool_calls=MAX_TOOL_CALLS)` — constructed per-call by `_orchestrate` so tests that swap `chat_system.text_engine` post-init still take effect. **DP-335 split the two limits**: `max_tool_calls` is the turn's budget (counted in calls executed, so it means the same thing on a batching and a one-call-per-message model), `max_iterations` is the pure runaway guard on provider round trips.
- `run(persona, conversation_history, params, tools, ...)` — async generator. Drives `text_engine.stream_messages` per iteration, forwards `TokenEvent`s, surfaces tool calls as `ToolCallStartEvent` / `ToolCallResultEvent`, mutates `conversation_history` in place (orchestrator + resume path read it back).
- **The assistant history entry keeps its prose (DP-338).** Each tool-calling iteration appends `{"role": "assistant", "tool_calls": [...]}` **plus a `content` key** holding whatever the model wrote beside its calls (key omitted entirely when there is none, so a call-only iteration keeps the old two-key shape). That prose is the plan for the batch; without it the next iteration re-read a transcript in which the calls had no stated reason, re-derived the plan from an identical history, and re-emitted the same batch. Providers park the prose in two places, so the loop reads both: the streaming ones delta it out and zero `full_text` on a tool turn, while agy's one-shot route emits no deltas at all and carries it on `done`. The same value feeds `audit_info.model_reasoning` — reading the deltas alone meant every agy-backed persona parked its writes with a blank "why" on the operator's dialog. The prose is run through the **egress scrubber before it enters history** (DP-225 boundary 2): it is sealed into `tool_context`, persisted to `User_Interactions` and replayed to the provider next turn, so it is a persistence path in its own right — scrubbing only `audit_info` below it would show the operator a redacted sentence while storing the unredacted copy. The Google history builder emits it as a leading `Part(text=...)` in front of the `function_call` parts, or the route would store the plan and strip it again on the way to the wire.
- Implements **universal write-audit**: on any iteration where a batch contains a `WRITE_TOOLS` call, the loop runs the read calls, then emits one `ToolDeferredEvent` **per write call** (each with its own token + `audit_info` block: per-action `irreversible` / `always_confirm` / `service_binding` / `sensitivity` / `enrichment`, plus turn `tainted` / `taint_sources` / `model_reasoning`). This is unconditional — execution mode is recorded in `audit_info` for display only, never gates parking. Taint (`turn_tainted` from `produces_untrusted` read tools) and irreversibility flags are computed here for the audit surface.
- **DP-297: parking is no longer a terminal exit.** The loop answers each gated call inline with a synthetic `awaiting_human_approval` tool result (DP-345: constructed by `awaiting_status(kind)`; the approval literal is **frozen** because it is already in durable `tool_context` blobs, every other kind is `awaiting:<kind>`, and consumers must test it with `is_awaiting_status()` rather than `== PARK_STATUS_AWAITING` — an `==` check answers False for every other kind, which reads as "this call completed") and *falls through to the next iteration*, so the model keeps working — and may propose again — while the operator decides. A turn can therefore park several writes and still end on ordinary text. `write_call_identity` (name + canonicalized arguments, id excluded) backs a duplicate guard so a model ignoring the "do not re-submit" instruction cannot turn N iterations into N copies of one affordance; the suppressed call gets a `duplicate_of_pending` result and is patched when the original resolves.
- Tool errors (returned as `{"error": ...}` by `tool_manager.execute_tool`) surface via `ToolCallResultEvent.error`; the loop continues so the model can adapt instead of hard-stopping.
- Internal events `_ApiPayloadEvent` / `_LoopFinishedEvent` / `_ToolContextEvent` / `_WriteDuplicateEvent` are loop-private — they let the orchestrator handle api-payload caching, write-audit parking, duplicate-entry patching, and assistant-row persistence without leaking into the public event surface. `ToolDeferredEvent` is the one non-underscore loop event, because interfaces consume it. **DP-345 renamed it from `WriteParkedEvent`** and gave it `kind`: the rename is the ticket, not cosmetics — every name on this mechanism said "write" and "approval", so the two later subsystems that needed exactly it (fixr's agent events, DP-343's node-job ping) could not find it and built their own.
- **Iteration-cap text (DP-335).** `render_max_iteration_text(history, start, max_iterations)` renders the DEV_COMMAND reply for the `max_iterations` exit from the same history slice sealing uses: one numbered line per tool call with arguments (scrubbed), outcome, and a `(same call as #N)` marker for verbatim repeats. Replaces the canned "stuck in a loop" sentence, which named no tool and read as a fault even when every call had returned `ok`. Full rationale: `docs/tool_loop_exits.md`.
- **Tool-context sealing (DP-296).** `seal_tool_context(history, start, reason)` serializes this turn's tool messages and synthesizes a `{"status": "not_executed", "reason": ...}` result for every `tool_call` left unanswered (`awaiting_approval` / `error` / `max_iterations_exceeded`). It does not mutate `conversation_history` — the continuation path still needs the unsealed list so real results can land. Every exit persists through it: the clean finish, the park, both error paths, and the iteration cap. Before this, only the clean finish persisted, so a gated or errored turn lost every tool call it made and the model saw nothing but its own prose on the next turn. Sealing exists because an unpaired `tool_call` block is *unsendable* — both Anthropic and Gemini reject it, which is why these exits used to store `None` rather than the raw slice.
- The error paths have no `_LoopFinishedEvent` to carry the context, so they emit `_ToolContextEvent` first; `_orchestrate` siphons it exactly like `_ApiPayloadEvent` and commits an assistant row before returning on `ErrorEvent`.
- Park-status vocabulary the *model* reads in replayed history (`PARK_STATUS_*`, defined here, distinct from `memory_manager`'s `PARK_DB_*` row states): `awaiting_human_approval`, `approved`, `denied`, `approved_but_failed`, `expired`, `interrupted_by_restart`, `quarantined_unreadable`, `already_resolved`, `duplicate_of_pending`.

### `src/engine/` — TextEngine + Provider family (DP-244)
- Provider-agnostic LLM abstraction: OpenAI, Anthropic, Google (Gemini/Gemma), local kobold-native, Antigravity (`agy`), Claude Code (`cc-*`)
- **Package layout (DP-244, MERGED):** `src/engine.py` (the old 1586-loc god module) is now the `src/engine/` package. Public import surface is unchanged — `from src.engine import TextEngine, LLMCommunicationError` still works.
  - `driver.py` — the slimmed `TextEngine`: routing, rate limiting, the empty-response retry loop, and 429 model-fallback policy. Owns image policy (`model_supports_images`) and `_FALLBACK_MODELS`.
  - `providers/{base,openai,anthropic,google,local,agy,cc,_shared,_subprocess}.py` — the `Provider` ABC (`base.py`) plus 6 fully-extracted providers, each owning its own streaming / message-build / tool-parse. `agy` and `cc` share the subprocess plumbing in `_subprocess.py`; `_shared.py` holds common helpers.
  - `registry.py` — `ProviderRegistry` / `build_registry(engine)`: the ordered provider list.
- **Provider routing (DP-244):** routing is `ProviderRegistry.resolve(model_name)` (registry.py), which walks an ordered list of `Provider` objects and returns the first whose `matches()` is true (after running its `ensure_supported` host guard). Order preserves the old waterfall precedence — notably `cc-*` before `claude`. Each provider is a fully-extracted `Provider` owning its own canonical streaming generator, emitting the unified event shape `api_payload → text_delta* → [tool_calls] → done`. The policy driver `_stream_response` wraps that stream with image policy, rate limiting, the empty-response retry loop (attempts emit nothing until they produce real content, so invalid attempts retry invisibly), and 429 model fallback. `_get_provider_route` and the `_stream_<provider>_response` methods survive only as back-compat shims delegating into `self._registry` / the resolved provider; the transitional `_EngineProvider` wrapper is retired.
- Entries: `stream_messages()` (chat pipeline — true token deltas; `local` goes straight to `StreamEngine.stream_messages` with GenerationParams/provider_extras intact and no retry policy) and `generate_response()` (one-shot = `collect_stream(_stream_response(...))`; used by agents/BotLogic/consolidation)
- `generate_response()` -> returns `Tuple[Dict[str, Any], Optional[Dict[str, Any]]]` (response, payload)
- Response dict: `{"type": "text", "content": "..."}` or `{"type": "tool_calls", "calls": [...]}` — the latter gains an optional `"content"` when the model wrote prose beside its calls (DP-338). `collect_stream` and `_events_from_one_shot` are inverses and both carry it: the one-shot synthesizer puts it on `done`, never on `text_delta`, because it is the plan for a batch that has not run yet, not an answer to stream at a user. `collect_stream` reads the deltas **first** and falls back to `done.full_text` (`or`, not `is not None`) — every streaming provider deltas the prose out and then reports `done` with `full_text: ""` on a tool turn, so preferring a merely-non-None `full_text` picked the empty string and dropped the prose for everything except agy.
- Per-model-family rate limiters (AsyncLimiter) configured in global_config
- `LLMCommunicationError` lives in the `src/llm_errors.py` leaf (shared with `stream_engine` without a cycle); `src.engine` re-exports it
- `src/stream_engine.py` (StreamEngine) is NOT a peer engine: it is the kobold-native `local` transport component, constructed by TextEngine itself (chat-template rendering, `<tool_call>` text protocol, per-token SSE; `TextEngine.aclose()` releases its HTTP client). Renders via the `CHAT_TEMPLATES` registry built from kobold-sourced instruct presets (`_KOBOLD_INSTRUCT_PRESETS`) plus a verbatim `alpaca` preset; the per-persona `chat_template` selects the preset (default `chatml`) and per-call `instruct_tags` override it. Exposed to the portal via `GET /api/v1/chat_templates` (DP-140). The old local OpenAI-compat one-shot transport is gone — local one-shot and streaming share one transport and one tool protocol.
- Wire-payload goldens: `tests/test_engine_payload_parity.py` pins the exact request kwargs per provider (frozen dicts; only capture points may move)
- **`agy` route (DP-127, model prefix `agy-*`):** drives the local `agy` CLI via subprocess on the OAuth tier (Gemini 3.5 Flash), not an API key. `_render_agy_prompt` flattens the full history into one role-tagged transcript (stateless, re-flattened each tool-loop turn); `_render_agy_tool_protocol` injects tool descriptions + asks the model to emit one or more `<tool_call>{json}</tool_call>` blocks; `_parse_agy_tool_call` parses **every** well-formed block back into the standard `{"id","name","arguments"}` shape so DERPR's own tool loop executes them (engine keeps full policy/CONFIRM/taint control — agy never runs tools itself), and the prose beside the blocks rides back on the result's `content` for the tool loop's history entry. **DP-338:** this used to be a first-match `re.search`. Blocks 2..N were dropped with no execution, result, error or log line, so a model told to batch its independent reads (which hypr's prompt explicitly asks for) saw one of N requests answered and re-emitted the same batch — and since block 1 never changes, that is a fixed point which spins until the turn's call budget trips. Observed in prod as 15 identical `pve_status` calls over 15 iterations. A malformed block among well-formed ones is now skipped rather than failing the response (and **logged** — `strip_tool_call_blocks` removes it from the prose too, so without a log line the dropped call is invisible in `calls`, in `content`, in the transcript and in the logs at once); a response whose blocks are **all** malformed still returns `None`, because that is the signal the empty-response retry keys off. The **field policy matches `_ToolCallStreamParser._commit_call`** as well as the extraction: `name` is required, `arguments` defaults to `{}` (a no-arg tool's block is legal), a stringified args object is JSON-decoded and a non-object is coerced to `{}` — handing a `str` to `execute_tool(name, **args)` raises TypeError inside the loop, which spends a budget slot to return "Tool execution failed". The streaming parser (`_ToolCallStreamParser.finalize`) always returned every block, so local models were never affected — agy's one-shot path was the only first-only parser. `_run_agy_cli` spawns with `stdin=DEVNULL` (or it blocks on an interactive permission prompt), **no** `--dangerously-skip-permissions`/`--add-dir` (so agy's own tools stay gated and it can never run them — DERPR drives every tool itself), in a throwaway tempdir, killing the process tree on cleanup. **Platform-agnostic since DP-324:** the route used to be POSIX-only (`_ensure_agy_supported()` refused native Windows, where older agy builds wrote their response only to a TTY while the engine captures piped stdout, so the response was always empty). agy >= 1.1.9 writes to a pipe on Windows, so the guard and its engine seam are gone and `AgyProvider.ensure_supported` is the base class no-op. The platform split now lives entirely in `_subprocess.py`: `_spawn_isolation_kwargs()` (POSIX `start_new_session` via setsid vs Windows `CREATE_NEW_PROCESS_GROUP` — `start_new_session` is silently ignored on Windows, so passing it there would look like isolation while providing none) and `_adopt_process_tree()` / `_kill_process_tree()`. **`CREATE_NEW_PROCESS_GROUP` buys only Ctrl-C isolation, not teardown** — `taskkill /T` walks parent-pid links from a snapshot and reaches the same descendants with or without the flag (verified), so it is not what makes cleanup work. Teardown: POSIX `killpg` unconditionally, including after a clean exit, because these CLIs leave background helpers behind; Windows gets the equivalent from a **job object with `JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE`** (`_adopt_process_tree` assigns the child right after spawn; closing the handle in the `finally` kills whatever is still inside, live parent or not — and the kernel closes it for us if derpr dies). `taskkill /F /T` is only the fallback when the job assignment failed, is skipped once the process has exited (the walk needs a live parent pid), and is spawned **without waiting** — blocking in that `finally` would stall the one event loop serving Discord, the portal and every other provider. `cc` remains POSIX-only while `CC_SANDBOX` is on — that guard is about Claude Code's OS sandbox, not about stdout. Text-only (no images). **Prompt is argv-bounded (DP-299):** the whole prompt is one argv entry (`agy --print <prompt>`; the CLI has no stdin/prompt-file transport) and `execve` caps a single argument at `MAX_ARG_STRLEN` = 128 KiB, while the engine bounds history by *tokens* (~131k ≈ 0.5 MB) — so `providers/_subprocess.fit_cli_prompt` clamps the prompt to `cli_prompt_budget()` (96 KiB on POSIX; 20 KiB on Windows, where `CreateProcess` caps the *whole* command line at `MAX_COMMAND_LINE_CHARS` = 32767 chars — a shared pool across every argv entry, so the cc route's `--system-prompt` gets its own smaller `cli_arg_budget()`. Budgets are resolved per call, not frozen at import, so both platforms stay testable from either host). **The budget's unit is `_cmdline_cost`, not bytes:** Windows has no argv vector — Python renders one string with `subprocess.list2cmdline`, which quotes entries containing whitespace and escapes every embedded `"` as `\"`, so the OS measures the *escaped* form. Measured expansion: x1.00 plain ASCII, x1.14 a JSON tool-result transcript, x2.00 quote-dense. Budgeting raw bytes against that cap is how a prompt the trimmer believed fit still died at the spawn with WinError 206, so `_cmdline_cost` costs the escaped string and `_keep_head_within` binary-searches the cut point (no arithmetic maps a budget to an offset once cost ≠ length), keeping the preamble (system prompt + tool protocol) whole and eliding the oldest history **messages**. Elision is message-granular because it consumes `render_transcript_blocks` (one `TranscriptBlock` per message, `role` retained) rather than re-splitting the flat transcript on blank lines — message content can itself contain blank lines, so re-splitting yields fragments and cuts mid-message. When nothing fits, the newest *user* block wins over a newer tool result (in the tool loop the last block is the tool output; keeping it while dropping the question leaves the model a blob and no task), and truncation keeps the block's **head** so the `User:`/`Tool(name):` label survives. Without any of this the spawn failed with `OSError [Errno 7] Argument list too long`. `exec_cli` wraps spawn `OSError` into `LLMCommunicationError`, quoting the payload size **only** for the oversized-command-line failure (`errno.E2BIG` on POSIX; WinError 206 on Windows, which Python raises as a `FileNotFoundError` — matching on errno alone would report it as a missing binary) — `FileNotFoundError`/`PermissionError` are `OSError` subclasses and must not be reported as size problems. The same clamp guards the `cc` route, whose `--system-prompt` is a second argv entry bounded separately by `clamp_cli_arg`. The Antigravity *SDK* route was rejected (API-key-only, Model-A-native) — see `project/decisions/2026-05-29-agy-sdk-oauth-finding.md`.
- **`cc` route (DP-222, model prefix `cc-*`):** drives the local `claude` CLI via subprocess (one-shot; the `CcProvider` in `providers/cc.py` with a dedicated `_cc_limiter`, resolved ahead of the `"claude" in model_name` branch). `_cc_model_arg` maps `cc-sonnet`→`sonnet` (bare `cc-` falls back to `sonnet`). POSIX-only when sandbox is on (`_ensure_cc_supported`, gated by `CC_SANDBOX`). Structural parity with `agy` (subprocess-per-call, persistent per-persona/global workspace, dedicated rate limiter) but a deliberate divergence: cc **ignores** the engine's `tools` argument and lets Claude Code run its OWN sandboxed tools under `--dangerously-skip-permissions` bounded by the OS sandbox (Seatbelt/bubblewrap via `_build_cc_sandbox_settings`), returning final text — DERPR's tool loop does not wrap it. `_resolve_cc_workspace` precedence: per-call `cc_workspace_override` (used by DP-227 fixr to point at an agent's worktree) > `CC_WORKSPACE_DIR` > per-persona dir > global dir; `_cc_workspace_locks` serialize same-workspace runs. Config: `CC_SANDBOX`, `CC_ALLOWED_TOOLS`, `CC_MAX_TURNS`, `CC_WORKSPACE_*`, `RATE_LIMIT_CC_RPM`. **DP-314:** before argv is built (order is load-bearing — the sandbox `allowWrite` entry is derived from whether the notes clone exists), `utils/notes_workspace.prepare_workspace_notes` seeds `CLAUDE.md` into non-checkout workspaces and links the ONE shared notes clone in as `memory/`. Sandbox settings come from `utils/cc_sandbox.build_sandbox_settings`, shared with the fixr dispatcher.
- Each provider builds messages differently but all accept the same context_object format
- Tool message format (used across all providers): `{"role": "tool", "tool_call_id": "...", "name": "...", "content": "..."}`
- Google provider translates to Part-based format in `_build_google_history()`
- 429 errors fail fast (no backoff) to preserve quota
- **DP-200 slice C:** the inline `<tool_call>` text protocol (tags, prompt rendering, extraction, JSON decode) is shared between the agy/cc providers and `stream_engine.py` (local models) via `src/text_tool_protocol.py` — byte-equivalent wire output; streaming buffer + per-path id-minting deliberately stay local to each engine. `extract_tool_call_blocks(text) -> List[str]` returns every complete block in emission order and is what the complete-response path parses (`extract_first_tool_call_block` survives as a first-only wrapper); `strip_tool_call_blocks(text)` removes them, which is how that path separates the prose from the calls — the streaming parser gets the same split for free as `visible_text`.

### `src/stream_engine.py` -- StreamEngine (DP-206b)
- Kobold-native local provider; an **engine-owned transport, NOT a peer engine**. TextEngine constructs one and routes every `model_name=="local"` request to it. Serves both streaming and one-shot collect paths; `TextEngine.aclose()` releases its HTTP client.
- Targets KoboldCPP's native `/api/extra/generate/stream` endpoint for true per-token streaming (not the OAI-compat surface). The old local OpenAI-compat one-shot transport is gone — local one-shot and streaming share one transport and one tool protocol.
- Owns `CHAT_TEMPLATES`, built from kobold-lite's `instruct_*` constants (`_KOBOLD_INSTRUCT_PRESETS`) plus a verbatim `alpaca` preset. The active template is selected by the persona's `chat_template` field (default `chatml`), falling back to env `KOBOLD_CHAT_TEMPLATE`; per-call `instruct_tags` override it.
- Shares the inline `<tool_call>` text protocol via `src/text_tool_protocol.py` (same wire shape as the agy path).
- Surfaced to the portal via `GET /api/v1/chat_templates` (returns `sorted(CHAT_TEMPLATES.keys())`, DP-140).

### `src/message_handler.py` -- BotLogic
- Command dispatch: set, what, hello, goodbye, remember, add, delete, detail, dump_last, **dump_history**, help, update_models. (There is no `dump_context` command — that name appeared in both architecture copies and in `user_guide.md` but has never been in the dispatcher; the handler is `_handle_dump_history`, keyed `dump_history`.)
- Explicit dependencies (DP-202): no ChatSystem reference or import. Rebindable collaborators (`personas`, `visible_personas`, `text_engine`, `tool_manager`, model-catalog get/set) are injected as zero-arg providers; `turn_persistence` (dump caches) and `memory_manager` (trust/untrust) are stable instances. Wired by `ChatSystem.__init__` with closures over `self`; tests use `tests.helpers.make_bot_logic(state)`. Runtime-import rule enforced in `tests/test_module_boundaries.py`.
- `preprocess_message()` -> returns command result dict or None (passes through to LLM)
- `set` handlers modify persona state in-memory (model, prompt, tokens, context, temp, tools, memory_mode, etc.)
- `dump_history` -> FILE_RESPONSE format, shows the exact history file sent to the model (from the last cached API payload)
- `dump_last` -> summary of last API request with turn counts
- **DP-200 slice D:** most `_what_X`/`_set_X` handlers are generated from the `src/persona_fields.py` registry (~35 bespoke methods deleted). Only ChatSystem-dependent commands stay bespoke (`set model` / `set tools` need async LLM/tool lookups). Help text is generated from the same tables, which fixed a long-standing staleness bug.

### `src/origin.py` -- typed message origin (DP-277)
- Leaf value object (zero `src.*` imports, like `tool_policy`), so any layer can carry an `Origin` without a cycle. `Origin(transport, server_id, channel_id, author_id, operator)` plus the `ANONYMOUS` secure default every caller that asserts no authenticated origin falls back to.
- `operator` is set by the interface adapter from **transport-authenticated facts only**, never from caller-supplied request fields: Discord matches gateway-asserted ids against `OPERATOR_ALLOWLIST`; the portal uses a validated `DERPR_CONTROL_TOKEN`; ticket bodies / Gmail / unauthenticated portal calls are structurally `operator=False`.
- `parse_operator_allowlist(raw)` → `(server, channel, author)` tuples from `server_id[/channel_id[/author_id]]` entries; missing or `*` components match anything at that level, and **malformed entries are dropped with a warning** (a wildcard server is refused outright — it would grant every guild the bot is in). Fail closed: a typo narrows access, never widens it.
- `is_discord_operator(...)` matches those tuples; a missing `server_id` never matches, so DMs are out by construction.
- `is_origin_allowed(allowlist, origin)` (DP-330) wraps the matcher for the *persona* allowlist: empty allowlist = unrestricted, non-Discord transport = refused. The transport check is belt-and-braces — the portal receives a **caller-supplied** `server_id` in its request body, and an adapter that ever copied it into an `Origin` must not thereby widen a persona allowlist.
- Two gates consume this, asking different questions, and **both live in `BotLogic.preprocess_message`** — the addressing gate first (may this origin *address this persona* at all — `Persona.is_addressable_from`, DP-330), then the control-plane gate (may this *command* reconfigure things — `Origin.operator`, DP-277). Same function because it is the only seam all three message-bearing surfaces share; see the kernel section for why the addressing gate could not stay in `_orchestrate`.

### `src/persona_fields.py` -- declarative persona-field registry (DP-200 D)
- ONE table drives FOUR drift surfaces: BotLogic what/set handlers, the kobold PATCH route (`apply_patch_fields` + known-keys derivation in `_persona_patch.py`), generated help text, and the S4 inspector's `rejected_fields`. Adding a persona field = one registry row.
- `PersonaField` (frozen dataclass) shape: `name` (dev-command key) · `describe: Optional[WhatFn]` (None ⇒ not queryable via `what`) · `set_cli: Optional[SetFn]` (None ⇒ not settable via `set`) · `patch_key: Optional[str]` (JSON key on the PATCH route, if patchable) · `patch_apply: Optional[PatchFn]` (returns True if the value was rejected). Persona itself keeps validation/coercion.
- Current registry: **27 fields**, of which 25 are CLI-settable and 16 are PATCH-able. The two sets are deliberately not identical — `model` is PATCH-only (the `set model` CLI path needs an async LLM fuzzy-match lookup, so it stays a bespoke BotLogic handler), and `default_prompt` / `display_name` / `execution_mode` / `explicit_overrides` / `origin_allowlist` / `history` / `service_bindings` / `tool_policy` / `disposition`-adjacent knobs are CLI-only.

### `src/persona.py` -- Persona
- Stateful LLM config: model, system prompt, token limit, temperature, top_p, top_k
- True domain leaf since DP-204: zero `src.tools` imports. Holds a `ToolPolicy` (the `src/tool_policy.py` leaf, like `generation_params`) and the DP-128 quarantine STATE only (`security_block_reasons`, `is_security_blocked()`, `set_security_block_reasons()` pure mutator); the validation that produces the reasons lives in `src/tools/composition.py`
- `ExecutionMode`: AUTONOMOUS, CONFIRM — recorded in the audit surface for display, but **does not gate write parking** (all write tools park for audit regardless; see ToolLoop universal write-audit)
- `MemoryMode`: CHANNEL_ISOLATED, SERVER_WIDE, PERSONAL, GLOBAL, TICKET_ISOLATED
- `get_history_messages()` -> supports dynamic override via hello/goodbye commands (increments by 2 per turn)
- Service bindings list determines which tools and services are available
- `origin_allowlist: List[str]` (DP-330) — which origins may address this persona at all, in the `OPERATOR_ALLOWLIST` entry form `server_id[/channel_id[/author_id]]`. **Empty = unrestricted**, so every pre-DP-330 persona file is unchanged by construction. Matched by `Persona.is_addressable_from` → `origin.is_origin_allowed`. Persona imports `src/origin.py`, which is a leaf like `tool_policy`/`generation_params`, so the domain-leaf rule still holds. Privileged like `explicit_overrides`: CLI-settable via the operator-gated `set origin_allowlist` (**audited** — `origin_allowlist_change`, same edit boundary as `explicit_overrides_change`), and **deliberately not PATCH-able**.
  - `Persona._normalize_origin_allowlist` runs on **both** write paths (construction and `set_origin_allowlist`), so the field reads back identically however it was last written. It exists because the value comes from a hand-edited JSON file and cannot be trusted to match its declared `List[str]`: **ints are accepted** (a guild id is a number, and the raw value used to hit `','.join` and raise `TypeError`, which `load_personas_from_file`'s blanket `except` swallowed into a `None` return — dropping *every* user persona and letting the next mutating dev command save an empty list over the file); a **bare string is one entry**, not one per character; and entries are **parsed one at a time**, because joining them with `','` first let a comma *inside* an entry grant a guild nobody authored — a typo that widens.
    - **The entry grammar has one owner: `origin.parse_operator_allowlist_entry`**, with `origin.split_allowlist_entries` owning the separator. Both were re-derived at their call sites — the tokenizer was copy-pasted into `persona_fields._parse_string_list_arg` and the normalizer, and "the parser did not split this" was inferred as `len(parse_operator_allowlist(text)) == 1 and ',' not in text`. That proxy is the widening rule (*a comma is the separator, so an entry containing one is two grants glued together*) written somewhere it cannot be maintained: adding a separator in `origin.py` would silently convert those rejections into grants. `parse_operator_allowlist` is now a loop over the single-entry parser, so the env-var path and the persona path cannot disagree.
    - **A no-op `set` is not a mutation.** `set_origin_allowlist` returns early when the normalized result matches what is already loaded, so `mutated` — which drives both the audit row and a full `personas.json` rewrite — means "the field changed". `set origin_allowlist none` on a persona that never carried the key used to report True, writing an audit row with identical prior/new state and flipping `declared` forever, so `to_dict` began emitting `"origin_allowlist": []` for a persona whose policy had not changed.
  - **Fail closed on a wholly-malformed list.** Empty means unrestricted, so letting a typo'd list parse down to empty converted a restriction into a wide-open persona while `what origin_allowlist` still reported the authored entries. Instead the persona carries an unmatchable sentinel and is unreachable. Three things had to be true at once for that guarantee to actually hold, and the first cut got each of them wrong:
    - **The trigger is "something was supplied", not "something survived stringification"** — `if (authored or rejected) and not parsed`. Keying it off `authored` alone skipped the two shapes that never reach that list (`[None]`/`[True]`/`[["x"]]`, and blank-only `["", " "]`, which was `continue`d rather than rejected), so the branch never fired and the persona came out **unrestricted** on input already diagnosed as bad.
    - **Persistence must round-trip the fail-closed state**, so `store.to_dict` writes `get_origin_allowlist_for_persist()` — the *raw* authored value when unreachable. Writing the normalized list dropped the unstringifiable entries, saving `[null]` as `[]`; unreachable in memory, unrestricted after the next restart, and one `set temp 0.8` was enough to trigger the rewrite.
    - **Reporting must distinguish unreachable from unrestricted.** `origin_allowlist_is_malformed()` is also True when *some* entries parsed, so it cannot carry this alone; `origin_allowlist_is_unreachable()` (the sentinel is present) is what `what origin_allowlist` and the setter's reply branch on first. Both fail-closed shapes leave the normalized list empty and were being reported as "unrestricted (any origin may address it)" — the opposite of the truth.
  - **Lockout is real and one-way; the docs claimed otherwise.** `set origin_allowlist` targets the *addressed* persona and has no cross-persona form, so "run it while addressing a different one" — which `user_guide.md`, this file, the setter docstring and a test docstring all said — clears the **other** persona's restriction while the locked-out one stays locked. Compounding it, `is_origin_allowed` refuses every non-Discord transport once the list is non-empty, so setting one over the portal locks the portal's own `dev_command` route out on the next call. Real recovery: a Discord origin the list admits, or editing `data/personas.json` and restarting.
  - `origin_allowlist_is_declared()` distinguishes "never had the key" from "explicitly `[]`". `store.to_dict` keys off it so a persona shipped with `"origin_allowlist": []` — the operator's only in-file hint that the knob exists — is not erased by the first mutating dev command, while a persona that never carried the key still round-trips unchanged.
- `include_ambient_memory: bool = True` — whether to include ambient-channel memories in retrieval; controlled by `get_include_ambient_memory()`
- `thinking_level: Optional[str]` — passed through to engine config (e.g. `"minimal"` for Gemma extended thinking)
- `max_context_tokens: int` — total context budget (prompt + reserved response). Defaults to `global_config.DEFAULT_MAX_CONTEXT_TOKENS` (131072). Mirrors kobold-lite's `localsettings.max_context_length` slider semantic so the value round-trips on persona load.
- `chat_template: Optional[str]` — name of the instruct/chat preset used by the local (kobold-native) `StreamEngine` to render the prompt (e.g. `chatml`, `alpaca`, and other kobold-sourced presets incl. thinking variants). Exposed via `get_chat_template()`/`set_chat_template()`, flows through `get_config_for_engine()`, settable via the `set chat_template` dev command and persona GET/PATCH. `None` = engine default (`chatml`).
- **Hindsight bank fields (DP-255):**
  - `retain_mission` / `reflect_mission: Optional[str]` — per-persona missions for the persona's Hindsight bank, honoured only at bank creation (`acreate_bank`). Accessed via `get_retain_mission()` / `get_reflect_mission()`.
  - `disposition: Optional[Dict[str, int]]` — Hindsight extraction disposition `{skepticism|literalism|empathy}`, each clamped 1–5 via `_sanitize_disposition` (`_DISPOSITION_KEYS`; unknown keys dropped, out-of-range/invalid values logged and skipped). Accessed via `get_disposition()`.
- `long_term_memory: bool` — per-persona LTM toggle (gates retrieval/retain for this persona)
- `meta_visible: bool` — whether this persona is eligible for cross-persona fan-out recall (`MemoryRouter.list_visible_personas`)
- `inject_timestamp: bool` — prepend a timestamp to turns (default `True` for user personas / `False` for system personas)
- `ingest_bank: Optional[str]` — Hindsight bank target for the `ingest_path` tool
- `provider_extras` via `get/set/clear_provider_extra(provider, key)` — per-provider passthrough knobs (e.g. kobold `instruct_tags`)
- All of the above persisted in `personas/store.py` `to_dict` (and read back with old-config-safe `.get(..., default)` defaults)
- `get_config_for_engine()` -> returns dict consumed by TextEngine (includes thinking_level + chat_template if set)

### `src/tools/` -- Tool System
- `definitions.py` -- JSON schemas for all tools aggregated into the static `ALL_TOOL_DEFINITIONS` seed, which seeds a `ToolDefinitionRegistry` — the live catalog (DP-268): static seed plus post-import `register_tool_definition()` defs (MCP-discovered tools), with `unregister_tool_definition()` to remove them (refuses non-`dynamic` static defs). Name index + write-tool set maintained incrementally; read via `get_tool_definition()` / `get_tool_capabilities()` / `is_write_tool()`; consumers needing the live toolset call `get_all_tool_definitions()`. `MODEL_INCOMPATIBLE_TOOLS` dict.
- `tool_manager.py` -- ToolManager registry, `execute_tool()`, `register()` / `unregister()` handler registration pattern
- `composition.py` (DP-204) -- the DP-128 composition-validation seam: `resolve_policy_tools` / `validate_policy_composition` (used by the persona store at load) + `revalidate_persona_security(persona)` (used by BotLogic `set tools`/`set tool_policy`, which the web tools modal routes through, and by every live MCP (de)registration). Personas are validated WITH this, not BY themselves. `ToolPolicy` itself lives in the `src/tool_policy.py` leaf (no `src.*` imports) so persona can hold one without importing `tools/`.
- `classifiers.py` -- argument-aware reversibility classifiers (e.g. `add_note_to_ticket` is irreversible only when the note is customer-visible). Feed the write-audit `irreversible` flags so HITL gating is per-call, not per-tool.
- `ingest_path.py` (DP-118) -- the `ingest_path` tool + `IngestPathHandler` (registered in `bootstrap.build_tool_manager`). Ingests a markdown file or directory into the persona's Hindsight bank (target from `bank` arg or `persona.ingest_bank`), sha256-idempotent per bank (skips unchanged content unless `force`). Also exposes a turn-context-free `ingest_root(bank_id, root, glob, force)` used by the DP-292 memory-import route.
- Voice timer tools (DP-238, `service_binding: "voice"`, all `is_write: False`): `set_timer`, `list_timers`, `cancel_timer` — text-LLM-callable so dictated "set a timer" routes through the normal tool loop. Registered by `src/voice/tools.py`.
- Tool categories: read-only (search, get) vs write (create, update, close) -- all write tools are parked for human confirmation regardless of execution mode (universal write-audit, per the security framework)
- `WebSearchHandler` registered at ChatSystem init
- Service-specific tools registered via `ServiceIntegration.register_tools()`
- Memory tools: `submit_memory_summary` (observations[] + outlier_ids[], used by SqliteConsolidator), `drill_down_memory` (retrieves source messages for a summary), `update_core_memory` (edits core profile summaries)
- **`exfil_capable` capability (DP-263):** optional tool-capability flag, default `True`. A network tool that sets `exfil_capable=False` opts out of the exfil-composition policy (`tool_policy.py` Rules 2/3) — used when a write tool's egress carries no model-controlled payload (constrained args to trusted infra), so it is not a data-exfil vector and must not arm those rules. Destructive risk on such tools is still covered by `is_write` (parked for confirmation). Example: proxmox `set_active_model` (the arg is only a lookup key against the units discovered on the box — the handler sends the discovered unit name, never the caller's string) and `gpu_status` (no args at all). Validated as `bool` at import in `definitions.py`; enforced in `tool_policy.py:114` (`locality == "network" and caps.get("exfil_capable", True)`).

### `src/memory/memory_manager.py` -- MemoryManager
- SQLite with single persistent connection, `check_same_thread=False`
- All DB ops run via `asyncio.to_thread()` from async callers
- Thread-safe via `threading.RLock`
- Uses `sqlite-vec` extension for vector similarity search

**Layer split (DP-108 / Sprint 1 of memory_backend_abc):**
- **Transcript layer (stays on `MemoryManager`)**: system of record. `log_message`, history queries (`get_*_history`), version chevrons (`update_interaction_content`, `list_interaction_versions`, `swap_interaction_version`), suppression, audit (`log_audit_event`, `mark_trusted`, `mark_untrusted`), edit/dedupe entrypoints (`invalidate_summary`, `handle_message_edit`, `handle_portal_retry`).
- **Memory layer (delegated to `MemoryBackend`)**: semantic + episodic surface — embeddings, segments, summaries, agent actions, segment-failure tracking. `MemoryManager.__init__` accepts `backend: MemoryBackend | None` (defaults to `SqliteSemanticBackend(self)`). All semantic/episodic public methods on `MemoryManager` are now thin delegations to `self.backend`.
- The backend shares MemoryManager's connection / lock / `_suppression_filter` (single SQLite connection across both layers; transactions span them).
- Sprint 2 lands `HindsightBackend` against the same ABC; the new-shape methods (`retain_turn`, `recall`, `reflect`, `list_mental_models`, `ensure_bank`, `delete_bank`) are placeholders on the SQLite backend (NotImplementedError or noop) until Hindsight wires them.

### `src/memory/backend/` -- MemoryBackend ABC + impls
- `backend/base.py` — `MemoryBackend` ABC. Carries both legacy SQLite-shape methods (current contract) and new Hindsight-shape methods (placeholders Sprint 2 fills in). Dataclasses: `MemoryHit`, `Experience`, `MentalModel`, `ReflectResult`.
- `backend/sqlite.py` — `SqliteSemanticBackend(memory_manager)`. Wraps existing semantic + episodic SQL. Reaches back through `mm._lock`, `mm._get_connection()`, `mm._suppression_filter()` to share the single connection. Pure refactor — no behavior change.
- `backend/hindsight.py` — `HindsightBackend` (native REST via `HindsightRESTClient`). Retain path is fire-and-forget through one `asyncio.Queue` per `bank_id`. Worker uses **drain-on-tick** coalescing: awaits one item, then drains the rest currently queued and POSTs them as a single upstream `RetainRequest` (`{items: [...], async: true}`). Each item carries `content`, `tags`, `document_id`, `update_mode`, `timestamp`, optional `metadata`. `document_id` is derived from a scope key `{bank_id}:{channel_id}` via `_DocScopeStore` (sibling SQLite at `HINDSIGHT_DOC_SCOPE_DB`, default `data/hindsight_doc_scope.db`); a >24h gap between retains in the same scope opens a new document with `update_mode="replace"`, otherwise items append. `_TrustOverrideStore` (sibling SQLite at `HINDSIGHT_OVERRIDE_DB`, default `data/hindsight_overrides.db`) holds operator trust flips and an audit trail; recall post-filters hits through it. Both sibling stores resolve their paths through `global_config` and default under `DATA_DIR` — the store-path policy of DP-303. They previously derived paths from `Path(__file__).parent.parent`, which put them in the image layer, so every CI/CD rebuild silently discarded all trust overrides and their audit trail. `ensure_bank` sends `retain_mission` + `reflect_mission` (and optional `enable_observations` / `observations_mission`) — never the deprecated `mission` / `background` aliases. **Read/list surface (DP-292):** `list_banks`, `list_documents`, `get_document`, `delete_document`, `list_operations`, `get_operation` wrap the upstream bank/document/operation endpoints (client methods `alist_banks`/`alist_documents`/`aget_document`/`adelete_document`/`alist_operations`/`aget_operation`, routes verified against live prod OpenAPI). **Operations are bank-scoped upstream** — no global collection — and the ops-list query param is `type` (client kwarg `op_type`). Unlike recall/reflect (fail-soft to empty), these operator-facing reads propagate `HindsightAPIError`. On the ABC they are default-raising stubs; SQLite inherits the raise.

### `src/memory/router.py` -- MemoryRouter
Cross-persona fan-out recall. `recall_many(personas, query, **kwargs)` parallelises `MemoryBackend.recall` across N persona banks (bank id == persona name), then merges + dedupes the hits by `id` (highest score wins) and sorts by score desc, timestamp desc. Per-bank failures are logged and dropped; surviving banks still return results. `list_visible_personas()` returns personas with `meta_visible=True` — the eligibility set for fan-out when invoked by a meta-agent. Sprint 4 groundwork; **no production caller wires this in yet** (Sprint 5 metabank).

**Tables:**
- `User_Interactions` -- interaction_id, user_identifier, persona_name, channel, author_role (user|assistant|system), author_name, content, timestamp, zammad_ticket_id, platform_message_id, server_id, tool_context, parent_summary_id (FK to Memory_Summaries), reply_to_id (FK to self, links assistant response to user question)
- `Suppressed_Interactions` -- flags messages to exclude from history (FK to User_Interactions)
- `Message_Embeddings` -- interaction_id (PK, FK), embedding (BLOB), model_name, created_at
- `Interaction_Edit_History` -- edit_id (PK), interaction_id (FK to User_Interactions), old_content, edited_at (archive of prior canonical content, ordered by edited_at ASC, edit_id ASC)
- `Edit_History_Embeddings` -- edit_id (PK, FK to Interaction_Edit_History ON DELETE CASCADE), embedding (BLOB), model_name, created_at (L0 embeddings of archived content — travel with content across chevron swaps; no vec shadow since archives don't participate in k-NN)
- `Memory_Segments` -- segment_id, channel, server_id, persona_name, start_interaction_id, end_interaction_id, message_count, created_at, first_message_at, last_message_at
- `Memory_Summaries` -- summary_id, segment_id (FK), content, embedding (BLOB), model_name, created_at, summary_level (0=legacy, 1=episodic, 2=core), parent_summary_id (FK to self, for consolidation hierarchy)
- `vec_Message_Embeddings` -- sqlite-vec virtual table for KNN search on message embeddings (float[3072])
- `vec_Memory_Summaries` -- sqlite-vec virtual table for KNN search on summary embeddings (float[3072])
- `Agent_Actions` -- id, parent_id, agent_name, action_type, trigger_context, action_payload, outcome, outcome_payload, timestamp
- `Agent_Action_Contexts` -- action_id + context_type + context_value (multi-dimensional retrieval)
- `Audit_Log` -- security/write-audit trail (event_type, target_id, timestamp; indexes idx_audit_event, idx_audit_target) backing `log_audit_event` / `mark_trusted` / `mark_untrusted`
- `Proposals` (DP-282, self-managing queue DP-290) -- proposal_id (PK), created_at, expires_at, agent_name, action_type, action_args, rationale, taint, source_action_id, ticket_number, status (CHECK: pending|approved|denied|expired|executed|execution_failed|withdrawn), reviewed_at, reviewer, review_note, executed_at, execution_result. Indexes: idx_proposal_status, idx_proposal_acceptance, idx_proposal_pending_key (partial UNIQUE on agent/action_type/ticket_number WHERE pending — dedup)
- `Parked_Writes` (**DP-319** — the durable chat-park store; see `src/confirmations.py`) -- token (PK), created_at (REAL), status (CHECK against the `PARK_DB_*` vocabulary, rendered into the DDL from those constants so it cannot drift), user_identifier, persona_name, channel, server_id, write_call, call_identity, audit_info, confirmation_text, turn_tainted, parked_assistant_id, duplicate_refs, **kind** (DP-345), resolved_at, **resolution** (machine-readable outcome the duplicate guard filters on) and **resolution_reason** (the human sentence). Those last two are deliberately two columns: as one column, the approve path wrote constants and the expire path wrote prose, so every filter over expired/interrupted rows matched nothing — the same split `Audit_Log` already makes between `new_state` and `reason`. On a decided park, `write_call` / `audit_info` / `confirmation_text` are NULLed and only the hashed `call_identity` survives. Indexes: idx_parked_write_conversation (user_identifier, persona_name, status, created_at), idx_parked_write_status (status, created_at). **DP-345 added `kind` (NOT NULL DEFAULT `'approval'`) and nothing else.** The default is the whole backfill: every row written before the column existed was a write awaiting a human. **No index covers `kind`, and that is deliberate** — nothing filters on it in SQL (the only read of this table is the boot scan, which selects on `status`), so an index there would be write amplification over a column no WHERE clause mentions. ⚠️ If a future column ever does need one, it goes in the ALTER-TABLE migration block, **never** in the CREATE TABLE script — that script runs first and `CREATE TABLE IF NOT EXISTS` is a no-op on an existing DB, so an index over a migrated column declared there is built before the column exists and takes boot down on exactly the deployments that already have data. `confirmation_text` / `call_identity` / `duplicate_refs` stay approval-only and are unused by other kinds
- `Standing_Orders` (DP-281) -- operator-authored persistent instructions for `ManagrAgent`; status CHECK active/retired (retire-only, no delete), `agent` scope column DEFAULT 'managr'. Index: idx_standing_order_agent_status. The store rejects writes whose `source` is not in `ALLOWED_STANDING_ORDER_SOURCES` (`{"operator"}`)

Note: On startup `create_schema()` validates both vec table dimensions against `EMBEDDING_DIMENSION` and auto-syncs any rows missing from the virtual tables.

**Key indexes:**
- User_Interactions: idx_channel_timestamp, idx_platform_message_id (unique), idx_zammad_ticket_id, idx_persona_timestamp, idx_user_persona, idx_server_id_timestamp
- Memory_Segments: idx_segment_channel_persona
- Memory_Summaries: idx_summary_segment, idx_summary_parent
- Agent_Actions: idx_agent_name_timestamp, idx_agent_action_type, idx_agent_parent

**History methods** (all apply SQL LIMIT, fetch DESC then reverse to chronological):
- `get_channel_history(channel, persona_name, server_id, limit)` -- default mode
- `get_server_history(server_id, persona_name, limit)`
- `get_personal_history(user_identifier, persona_name, limit)`
- `get_global_history(persona_name, limit)`
- `get_ticket_history(ticket_id, limit)`

All exclude suppressed messages via `_SUPPRESSION_SUBQUERY`.

**Memory retrieval:**
- `retrieve_relevant_summaries()` -- KNN search via `vec_Memory_Summaries`, filters by channel/persona/model/level, returns top-K summaries by distance; accepts `exclude_after_interaction_id` (skip memories already in sliding window) and `include_ambient` flag
- `get_unembedded_messages()` -- msgs not yet in Message_Embeddings (Phase 1 of SqliteConsolidator)
- `get_unsegmented_embedded_messages()` -- finds embedded msgs with `parent_summary_id IS NULL` for segmentation (Phase 2)
- `get_active_channels()` -- UNION of 3 queries: unembedded msgs, msgs above segment high-water mark, embedded-but-unsummarized msgs
- `get_last_segment_tail_embeddings()` -- tail N embeddings from last segment (for centroid seeding)

`log_message()` -- inserts a row, accepts `reply_to_id` to link assistant response to user question
`suppress_message_by_platform_id()` -- marks a message for exclusion (called on Discord message delete)

### `src/memory/memory_consolidation.py` -- MemoryConsolidator
Promotes episodic summaries (level 1) to core profiles (level 2) by clustering similar summaries.
- O(n^2) pairwise similarity comparison (future: sqlite-vec KNN)
- Union-find clustering on summaries above similarity threshold
- LLM merges clustered summaries into consolidated core profiles
- Writes new level-2 summary with `parent_summary_id` linking back to source episodics

### `src/memory/context_budget.py` -- Context budget enforcement (Phase 3)
Pure helpers for enforcing per-persona `max_context_tokens`. **One callsite today: `request_builder.prepare_request`**, which calls `truncate_messages_to_budget` and then `drop_orphaned_tool_head`. (Both docs used to claim a second callsite in `kobold_engine_adapter` `/chat/completions`; that went away when Phase D moved budget enforcement engine-side — the adapter's OAI route is now a pure transcoder and the native passthrough routes prune nothing.) Future home for dynamic LTM-depth modulation, retrieval-score-weighted budget allocation, and a real tokenizer swap.
- `estimate_tokens(text)` -> `(len(text)+3)//4`. No tokenizer roundtrip.
- `truncate_messages_to_budget(messages, max_tokens)` -> `(pruned, dropped_count)`. Drops oldest non-system messages until total ≤ budget. Preserves all `role=='system'` messages and the most recent `role=='user'` message (so the current turn is never evicted). No-op when `max_tokens` is None / non-positive / already under budget.
- `drop_orphaned_tool_head(messages, injected=None)` -> `(repaired, dropped_count)` (DP-298). A history that *starts* on an `assistant`-with-`tool_calls` or a `tool` message whose triggering user turn is gone is rejected by Google (400 *"function call turn must come immediately after a user turn"* for a leading call; *"function response turn must come immediately after a function call turn"* for a leading bare result). **Backstop, not primary defense, for the DB path:** DP-296 made `truncate_messages_to_budget` treat an `assistant`+`tool_calls` message and its `tool` answers as one atomic span, and gave `build_conversation_history` a guard that refuses to replay `tool_context` unless a `user`/`tool` turn precedes it. The path where this is still load-bearing is **`client_messages`** (kobold-lite supplies its own array, taken verbatim apart from stripping one leading system message) — neither DP-296 guard runs there. The scan walks **through** a leading run of non-conversational preamble before looking for the orphan: `role=='system'` entries plus anything in `injected`, matched by **identity**. `prepare_request` passes the LTM recall block as `injected` — it is inserted at index 0 as a `user` message but is not a real conversational turn, and without the skip it shielded the orphan behind it. Identity matching means a caller can pass a block the pruner may or may not have dropped and get the right answer either way. Called once, last thing in `prepare_request`, after both truncations.
- Budget semantic is **kobold-style total ctx** (prompt + reserved response). Effective prompt prune budget = `persona.max_context_tokens - persona.response_token_limit`.

### `src/embedding_service.py` -- EmbeddingService
- `GeminiEmbeddingProvider` -- Google Gemini Embedding API (model: `gemini-embedding-001`)
- Token-aware chunking: splits text batches to stay under TPM limits
- Rate limiting via `GLOBAL_EMBEDDING_LIMITER` (shared AsyncLimiter)
- `encode_batch()` -- embeds multiple texts, returns list of BLOB embeddings (normalized float32)
- `encode_single()` -- single text embedding
- All embeddings L2-normalized at storage time → dot product = cosine similarity

### `src/interfaces/discord_bot.py`
- `create_discord_bot()` returns CustomDiscordBot (discord.Client subclass)
- `on_message`: filters out bot's own messages (line 150), thread messages (155), debug channel (151)
- Persona detection: message prefix match ("persona_name message") or channel name match
- Dev commands intercepted BEFORE generate_response (line 177) via `preprocess_message`, responses sent to threads via `_send_dev_response`, then returns -- never logged to DB
- LLM responses: logs user msg + assistant msg to DB via `memory_manager.log_message` (lines 257-290)
- Confirmation flow: reaction-based approval (✅/❌) via a **global `on_raw_reaction_add`** handler, NOT the inline `wait_for` this used to block on (removed DP-297 — a blocking wait allowed only one park per conversation, and the message a reaction lands on is now the only thing identifying which of N proposals the operator meant). `_confirm_registry` maps message id → `(token, owner id, persona)`; only the conversation's owner may resolve it. ⚠️ **raw, not cooked** (DP-319): `on_reaction_add` fires only for messages in discord.py's `max_messages` client cache, which is empty after a restart — the exact case durable parks created. `_post_pending_proposals` posts one message per unrendered park (registering the id→token map BEFORE adding reactions, so a bot without "Add Reactions" still yields an answerable proposal), tracked by `_rendered_park_tokens`; `_notify_stale_button` answers a click on a dead button, gated on the bot's own ✅/❌ being present so ordinary reactions are ignored.
- `on_message_delete`: calls `suppress_message_by_platform_id`
- Ambient logging: messages in AMBIENT_LOGGING_CHANNELS logged under persona="ambient"
- `_safe_typing`: gracefully handles 429 on typing indicator

### `src/interfaces/gmail_bot.py` (Proof of Concept)
- POC, not fully designed -- expect significant changes
- Polls Gmail API for new messages via history ID tracking
- Persona routing based on email recipient address
- Forces AUTONOMOUS mode (overrides CONFIRM since Gmail has no interactive confirmation)
- Does NOT call `log_message` -- no persistence currently

### `src/interfaces/kobold_engine_adapter.py` -- KoboldEngineAdapter (:5003, `KOBOLD_PORT`+1)
- FastAPI app served from `KoboldAdapter.start()`; serves the bespoke React portal at `GET /derpr` (DP-132+), mounts the customised kobold-lite at `/portal` (`web_assets/portal.html`), owns the OAI engine path, and forwards native inference traffic to local KoboldCPP.
- **⚠️ `src/interfaces/kobold_adapter.py` (the standalone :5002 passthrough app) was RETIRED in DP-200 slice A (−2630 LOC incl. tests).** What was retired is that whole app — NOT the native routes themselves. `POST /api/v1/generate` and `POST /api/extra/generate/stream` survive here as **DB-logging forwarders**: they forward verbatim to upstream KoboldCPP via `self._http` to `_kobold_base_url()`, with DB logging through `_log_interaction` / `_commit_assistant`. They prune nothing and sit off the OAI engine path.
- **DP-205 — enumerated engine dependency surface.** The adapter still takes `chat_system` in `__init__`, but routes never touch `self.chat_system.*` directly — all engine access goes through named read-through seam accessors declared in one block: 13 properties (`_personas`, `_visible_personas`, `_system_persona_names`, `_models_available`, `_memory_manager`, `_tool_manager`, `_bot_logic`, `_memory_backend`, `_confirmations`, `_stream_response`, `_assemble_request`, `_get_view_history`, `_get_session_memory_block`) plus the `_stream_resolve_park` method. Read-through properties (not eager destructuring) because the engine rebinds several post-construction (`models_available` on `update_models`, `personas`/`tool_manager` in admin/test paths). An AST test (`test_adapter_engine_surface_is_enumerated`) pins the surface: new ChatSystem reaches require a seam accessor + allowed-map entry. `_memory_backend` was added by DP-292; `_stream_resolve_park` replaced DP-124's `_stream_resume_confirmation` in DP-297.
- Inference path is OAI-jinja only: the portal runs kobold-lite in kcpp-with-jinja mode, which hijacks `opmode==4` to `/v1/chat/completions`. **Phase D (2026-04-28) split the adapter routes:** `/v1/chat/completions` is now a thin SSE transcoder over `chat_system.stream_response` — the engine rebuilds messages from DB, prunes to budget, archives on retry, and commits the assistant turn; the adapter only formats OAI-shape SSE chunks (and the `event: derpr` frame on `DoneEvent`). Client `data["messages"]` is discarded; `derpr_user_text` (or last-user fallback for non-portal clients) drives the user turn. The native `/api/v1/generate` and `/api/extra/generate/stream` routes remain verbatim passthrough to KoboldCPP because the pre-rendered kobold prompt cannot be safely reconstructed from DB (template-tag drift hazard, see `decisions/2026-04-19-portal-phase2-approach.md`); deprecation queued pending an OAI-feature-parity audit. CORS is wide open (kobold-lite reaches localhost from a different origin).
- Persona endpoints (`/api/v1/model`, `/api/v1/persona/...`, `/v1/models`) read/write through `chat_system.personas` and persist via `save_personas_to_file`. `POST /api/v1/personas` (DP-231) creates a new user persona from the portal's "+ New persona" form: name lowercased + validated as a single `[a-z0-9_-]` token (`_VALID_PERSONA_NAME`), 409 on collision, blank prompt falls back to `"you are in character as <name>"`, body applied through the shared `apply_persona_patch_body` chokepoint (same as PATCH), persisted via `save_personas_to_file` with in-memory rollback on save failure. `GET /api/v1/chat_templates` (DP-140) returns `sorted(StreamEngine.CHAT_TEMPLATES.keys())` for the inspector's template dropdown.
- Phase 2.1 adds `GET /api/v1/session/{persona}/kobold_export?max_turns=K` — returns a kobold-lite v1 savefile JSON built by `interfaces/kobold_export.build_kobold_savefile`. Pulls `chat_system.memory_manager.get_global_history(persona, K)` (no channel scoping; the portal has no channel concept). `K` defaults to the persona's `get_base_history_messages()` (the sliding-window size; no new config key).
- Phase 2.2 adds `GET /api/v1/session/{persona}/ltm_block?query=...` — delegates to the public `chat_system.get_session_memory_block(persona, user_identifier="portal", channel="web_ui", server_id=None, query=query)` seam (RequestBuilder rebuilds the history + recency cutoff internally) and returns `{"block": str | null}`. The portal wraps `prepare_submit_generation` async to fetch this before each submit, writes the block into kobold-lite's `current_anote` JS variable, and kobold-lite embeds it into the prompt at its normal author's-note position. No server-side prompt mutation occurs. Phase 2.2 also adds `memory_mode` to `GET /api/v1/persona/{name}` and accepts it in `PATCH /api/v1/persona/{name}`, wiring through to `persona.set_memory_mode()`.
- Phase 2.3a–b semantics (sidecar `derpr_user_text` for the user turn, `derpr_retry` for regens, `event: derpr` SSE frame carrying `assistant_id` for chevron hydration, partial-flush on abort) all survive Phase D — they are now implemented inside `chat_system._orchestrate` instead of in the adapter. The adapter handler is purely transcoding: maps `TokenEvent` → OAI `chat.completion.chunk`, `DoneEvent` → `event: derpr` frame + `[DONE]`, `ErrorEvent` → OAI error envelope. `_find_last_user_content(messages)` remains as the non-portal fallback when no sidecar is supplied. `_log_interaction` and `_commit_assistant` adapter helpers are still used by the native passthrough routes (`/api/v1/generate`, `/api/extra/generate/stream`); they are not on the OAI path anymore.
- Phase 3 `max_context_tokens` is exposed on `GET/PATCH /api/v1/persona/{name}`; budget enforcement on the OAI path moved to engine-side as of Phase D (`request_builder.prepare_request` calls `truncate_messages_to_budget` against the rebuilt history). The native passthrough routes do not prune.
- Phase 2.4 closes the portal edit/delete round-trip. `PATCH /api/v1/interaction/{id}` (body `{"content": str}`) calls `update_interaction_content`, which now ALSO drops the row's `Message_Embeddings` + `vec_Message_Embeddings` entries so `SqliteConsolidator._embed_unembedded` re-encodes against the new content on the next batch (the existing LEFT-JOIN-on-`Message_Embeddings` query naturally re-picks rows with stale embeddings cleared); `parent_summary_id` is set to NULL so the next summarizer pass re-groups the row. `DELETE /api/v1/interaction/{id}` calls a new thin wrapper `memory_manager.suppress_interaction(interaction_id)` that inserts a single `Suppressed_Interactions` row (idempotent — second call swallows `IntegrityError` and returns False; the endpoint surfaces this via `already_suppressed: bool`). Suppressed rows are filtered everywhere via `_suppression_filter` (history, sliding window, `kobold_export`, `get_unembedded_messages`, `get_unsegmented_embedded_messages`, `get_active_channels`); reply chains are kept intact (no FK cascade, no nulling of `reply_to_id`), and the segmenter handles orphaned `reply_to_id` references cleanly by falling back to the similarity gate. Portal: `edit_chunk_save` is wrapped to PATCH on non-empty edits and DELETE on empty edits; `interaction_ids[]` is spliced in lockstep with `gametext_arr`. **Archive bloat (option B — dedupe-on-archive)**: `swap_interaction_version` now content-hash-checks before inserting the canonical into `Interaction_Edit_History`; if a row with the same `(interaction_id, old_content)` already exists, the insert is skipped (chevron toggled back to a known content). Stale L0 rows are still dropped because the target restore step overwrites canonical content. This bounds chevron-toggle churn to the unique-content count rather than the click count; genuinely unique edits still grow the archive linearly (intended).
- Phase 2.3b adds version navigation backed by `Edit_History_Embeddings`. Schema: `Edit_History_Embeddings(edit_id PK → Interaction_Edit_History ON DELETE CASCADE, embedding BLOB, model_name, created_at)` — L0 embeddings travel with content across swaps; archives do not participate in k-NN (no `vec_` shadow). Endpoints: `GET /api/v1/interaction/{id}/versions` → `{interaction_id, versions: [{edit_id, content, created_at}, ...]}` with archives ordered `(edited_at ASC, edit_id ASC)` and canonical synthesized last (`edit_id=None`); `POST /api/v1/interaction/{id}/select_version/{k}` invokes `memory_manager.swap_interaction_version(interaction_id, k)` which performs archive-current + restore-target atomically in one txn — archives current canonical (new `Interaction_Edit_History` row, `Message_Embeddings` → `Edit_History_Embeddings`, delete `vec_Message_Embeddings`), restores target archive at position `k` (copy `old_content` → `User_Interactions.content`, move `Edit_History_Embeddings(target)` back into `Message_Embeddings` + `vec_Message_Embeddings`, delete target archive row). Returns `{current_content, interaction_id, total_versions}`; raises `IndexError` (400) on out-of-bounds k and `ValueError` (404) on unknown id with no state mutation. The stream relay in `_setup_routes` now detects `[DONE]` in the upstream chunk, commits the assistant buffer first, and emits `event: derpr\ndata: {"assistant_id": N}\n\n` immediately before forwarding `[DONE]` so the portal can hydrate chevron stacks (`retry_prev_text` / `redo_prev_text`) via `/versions`. Error and abort paths skip the frame (no chevron-able assistant turn). Portal-side: a `window.fetch` wrapper tees the `/chat/completions` response body, parses SSE blocks out-of-band to catch `event: derpr`, and on receipt calls `_derprHydrateVersions(assistant_id)` which fetches `/versions`, populates `derpr_version_chain` + `derpr_cursor`, and derives `retry_prev_text` / `redo_prev_text` from the chain. `btn_back` / `btn_redo` are wrapped to POST `/select_version/{k}` (k looked up by content-match against the current server version list) and advance the cursor; original kobold behavior is the fallback when no assistant_id is known. The kobold-side 10-entry cap on `retry_prev_text` has been removed — full regen history lives in the DB. A `PATCH /api/v1/interaction/{id}` route is also exposed (body `{"content": str}`) for manual edits from the portal; it calls `update_interaction_content` directly and does not archive (the prior content is whatever the caller wants replaced).

- **Phase 4.1 — history contract (DP-130, server port-forward).** Replaces the brittle positional `interaction_ids[]` shadowing (Phase 2.4) with a server-authored per-chunk identity contract. Three server-side pieces (portal.html left untouched — that re-keying is DP-131's Lite stopgap; the bespoke UI is DP-132+). Full spec + invariants C1–C5: `project/decisions/2026-06-02-portal-history-contract.md`.
  - **id-frame on EVERY turn.** `DoneEvent` gained `ephemeral_chunk_id: Optional[str]`. The OAI relay in `kobold_engine_adapter` now emits `event: derpr\ndata: {"user_id", "assistant_id", "response_type", "ephemeral_chunk_id"}\n\n` before `[DONE]` on *every* terminal turn — including a parked CONFIRM write (`assistant_id=None`), a tool-only turn, or an empty generation. Previously the frame was emitted only when `assistant_id is not None`, so parked/tool-only/abort turns sent no frame and the portal's positional id array drifted vs the visible story (root cause of "dispatchr is useless in the web UI"; live evidence `ids_len=11 storyLen=13`, `actions=12 ids=13`). The frame shape is **frozen** as of DP-130 — DP-131/DP-132 consume it.
  - **`ephemeral_chunk_id`.** A stable handle for the rendered-but-unpersisted confirmation chunk on a parked turn. It is the parked `ParkedWrite.token` (uuid4 hex; reconciles with the DP-127-engine confirm-modal `token` — same correlation id the interactive surface sends back on approve/deny). `ParkedWrite` also carries `confirmation_text` (the parked chunk's rendered text) so a fresh load can render the awaiting-approval text. Non-parked turns carry `ephemeral_chunk_id=None` and are addressed by `assistant_id`.
  - **`GET /api/v1/session/{persona}/transcript`** — the authoritative projection: `{"chunks": [...]}`, each chunk `{interaction_id|null, role, content, ephemeral, reasoning, tool_context, has_versions}` (invariant **C1**: exactly one `interaction_id` OR `ephemeral=true`). Same persona/global-history scoping as `kobold_export`; suppressed rows already filtered upstream (**C5**). `has_versions` comes from a single batch query `memory_manager.get_ids_with_versions(ids)` (no N+1). A live parked confirmation for `("portal", persona)` is appended as a trailing ephemeral chunk carrying its `ephemeral_chunk_id`. This is the re-sync source for the Lite stopgap and the render source for the bespoke UI — consumers address chunks by identity, never by position, so the array can no longer drift.
  - **`build_kobold_savefile` C2 fix** — see kobold_export section below.
  - **What converges now vs DP-131 (DP-130 is server-only):** the gametext-aligned `kobold_export` (C2) makes the **session restore/reload** path converge with the *unchanged* portal today (`ids_len==storyLen`, verified live). What still needs DP-131 is the **in-session per-turn** path: portal.html line ~33349 guards id pushes on `if (payload.assistant_id)`, so a parked turn mid-session won't advance the array until the next reload — DP-131 drops that guard / re-keys off `/transcript`. The contract test `test_parked_write_emits_id_frame_with_ephemeral_chunk_id` proves the on-wire parked frame deterministically through the real kernel.

- **DP-292 — memory import panel routes.** Operator inventory + ingest for Hindsight banks, all under `/api/v1/memory/*`, reached through the enumerated `_memory_backend` seam (`chat_system.memory_backend`; pinned by `test_adapter_engine_surface_is_enumerated`). Reads are GET (open); mutations pass the DP-277 control-plane auth middleware (`DERPR_CONTROL_TOKEN`). Routes: `GET /banks`, `GET /banks/{id}/documents` (query `q`/`tags` CSV/`limit`/`offset`), `GET /banks/{id}/operations` (query `status`/`type`→`op_type`), `DELETE /banks/{id}/documents/{document_id:path}` (path converter — relpath keys hold slashes), `POST /banks/{id}/upload` (multipart, `.md`/`.txt` decoded → `retain_document`, filename-keyed idempotency; PDF/native `files/retain` deferred), `POST /banks/{id}/ingest_url` (adapter `self._http` fetch → `retain_document`), `POST /banks/{id}/ingest_path` (constructs an `IngestPathHandler`, calls the new turn-context-free `ingest_root(bank_id, root, glob, force)`). Backend failures are mapped by the static helper `_run_backend`: `NotImplementedError`→501 (SQLite backend has no import surface), `HindsightAPIError`→upstream status, `httpx.RequestError`→502. The read routes hit `NotImplementedError` naturally; the `upload`/`ingest_url` routes instead call `_import_surface_501()` up front (SQLite `retain_document` is a silent no-op returning `None`, so without the guard they would report `accepted` while storing nothing — `ingest_path` keeps its own noop-note contract via `IngestPathHandler`). Frontend: the `◈ MEMORY` NavRail dock flips a `view: 'chat'|'memory'` state in `App.tsx` to render `components/MemoryPanel.tsx`; live-only client fns in `api/client.ts` (`listMemoryBanks`, `listMemoryDocuments`, `listMemoryOperations`, `deleteMemoryDocument`, `uploadMemoryFiles`, `ingestMemoryUrl`, `ingestMemoryPath`) — no mock fallback, mutations carry `withAuth`.
- **DP-292 phase 2 — content-date anchoring.** Hindsight derives every extracted memory's `mentioned_at`/`event_date` **solely from the retain request's `timestamp`** (the extraction LLM never reads dates from prose — measured, see `2026-05-14-hindsight-backfill-timestamp-finding`). So the three text-ingest paths (upload, `ingest_url`, `IngestPathHandler`) resolve an anchor date from the document body *before* retain and pass it as `timestamp` instead of upload-time/mtime. `src/memory/date_extraction.py` owns the deterministic core: `extract_regex_dates(text)` scans ISO / `YYYY/MM/DD` / named-month forms (bare-numeric `MM/DD/YYYY` intentionally excluded — locale-ambiguous), `pick_anchor(dates, clamp_now)` drops future (`> clamp_now + 1d`) and pre-`1990` values and returns the **latest** survivor, and `async extract_anchor_date(text, *, fallback_ts, clamp_now=None, llm_tagger=None) -> (datetime, source)` runs regex first, then the optional `llm_tagger` when regex is empty, then `fallback_ts`; `source ∈ {"regex","llm","fallback"}`. The optional LLM fallback is `src/agents/date_tagger.py::DateTagger` — a **single-shot inference agent**: a synchronous, caller-invoked forced-tool-schema (`submit_date`) call on the stateless, tool-less `date_tagger` system persona, mirroring `ContentClassifier`. Body handed in as untrusted DATA, output constrained to one ISO date or `none`, validated + future-clamped on our side (injection can at worst move the anchor to a plausible past date, never emit instructions or reach a tool). **Wiring (DP-292 follow-up):** this is a second agent *shape* — it does not fit the scheduled-loop `Agent`/`start_agent` path (nothing to schedule), so `AgentManager` gained `register_inference_agent`/`get_inference_agent` (DI-built on lookup via the shared `_di_kwargs`, cached, never launched as a task). `main.py` registers `date_tagger`, builds its `.tag` callable when `DATE_TAGGER_ENABLED`, and **injects the callable** into `create_kobold_engine_adapter(date_tagger=…)` (and thence the route-built `IngestPathHandler`). The interface/tool layers never import `DateTagger` — they receive a structural `LlmTagger` callable, honoring the `interfaces↛agents` / `tools↛agents` boundaries. This gives single-shot agents the same convention-DI (`notification_router`, `zammad_client`, …) as scheduled agents — the path a future "DM the operator" feature uses instead of hand-threading a router into a route handler. **DP-294** migrated `ContentClassifier` onto this seam: `main.py` registers it under `CONTENT_CLASSIFIER_NAME`, and `_di_kwargs` now injects any registered inference agent whose name matches a consumer's `__init__` param, so `ZammadBot(content_classifier=…)` receives the shared cached instance by DI instead of constructing it ad-hoc from `chat_system`. The dead `create_zammad_bot` factory (last ad-hoc construction site, unused) was removed. Each retained doc gains `date:<YYYY-MM-DD>` + `date_source:<...>` tags and the same metadata keys. Anchoring is per-document (one item → one `timestamp`; Hindsight stamps every unit it extracts with it — per-fact granularity is not available upstream). The model-callable `ingest_path` tool (built in `src/bootstrap`) does regex-only anchoring (no injected tagger). **Format-gap DM (DP-292):** because a successful LLM fallback means the regex lacks a format a real doc used, `DateTagger.tag` fires a side-effect notification via its DI'd `notification_router` — the `submit_date` schema carries a `source_text` (verbatim string the date was read from; ISO output alone can't name the format), the report is sent only when `source_text` actually occurs in the body (anti-hallucination), and it is deduplicated by digit-masked shape (`_format_shape`: lowercase, digits→`#`, ws-collapsed) persisted to `DATE_FORMAT_REPORTS_FILE` so a bulk ingest of one unmatched format yields one DM. Recipient resolves via the `date_tagger` `agents.json` block's `notification_defaults` + shared `_recipients` (mirrors dispatch/reminder). This is the first consumer of the DP-292 single-shot-agent DI: `notification_router` + `agent_config` arrive by convention-injection, no router hand-threaded through the ingest route.

### `src/interfaces/kobold_export.py` -- Savefile builder + transcript projection
- Pure function `build_kobold_savefile(raw_history)` → `(savefile_dict, skipped_count)`.
- **Invariant C2 (DP-130) — gametext alignment:** `interaction_ids` carries one entry per *visible story chunk* including the prompt, so `len(interaction_ids) == len(actions) + 1` (= kobold-lite's `gametext_arr` length, `[prompt, *actions]`). This is the alignment the portal actually uses — `derpr_interaction_ids[modified_turn]` is keyed by the gametext index (index 0 == prompt), and the SSE id-frame path pushes one id per visible chunk. The loop appends a slot (id or `None`) for every renderable row and **never pops the id array**; only `rendered[0]` is split off into `prompt`. **Correction (this is the real C2):** the decision doc's literal `len(actions)==len(interaction_ids)` was itself off-by-one — the reported `actions=12 ids=13` was *correct* gametext alignment (13 chunks = prompt + 12 actions). The actual pre-fix defect was a conditional `if isinstance(iid,int)` append that could drop an id without dropping its chunk; the gametext-aligned rewrite fixes that and makes the **unchanged portal restore correct today** (verified live: `ids_len==storyLen==7`, was 5 vs 6 under a transient actions-aligned implementation). Renderability is shared with the transcript via `_is_renderable` / `_merge_reasoning` helpers.
- `build_transcript(raw_history, *, ids_with_versions, pending)` → `{"chunks": [...]}` — the DP-130 projection (see Phase 4.1 above). Shares the row-filter helpers with the savefile builder so both stay consistent. `pending` (the live parked confirmation, when present) is appended as a trailing `ephemeral=true` chunk.
- Wraps `author_role='user'` rows in the literal placeholders `\n{{[INPUT]}}\n…\n{{[OUTPUT]}}\n`; emits `author_role='assistant'` rows as raw text. System rows, empty-content rows, and unknown-role rows are counted once in `skipped` and dropped. Assistant rows with `tool_context` are treated as normal rows — they're skipped only when content is empty (tool-call-only), otherwise their visible text is preserved. Proper tool-turn rendering is backlog.
- The placeholders mirror kobold-lite's `instructstartplaceholder` / `instructendplaceholder` constants; kobold's render layer substitutes them with the active `instruct_starttag` / `endtag` at submit time, so DERPR never picks the actual instruct tags.
- The first emitted entry becomes the savefile's `prompt`, the rest go into `actions[]`. `memory` is left empty — the persona system prompt is routed into kobold-lite's `instruct_sysprompt` (Sys. Prompt) setting by the UI's `applyPersonaToUI`, keeping the Memory block user-owned.

### `src/interfaces/portal_render.py` -- portal HTML server
`render_portal_html(portal_type)` serves kobold-lite's `portal.html` with `window.DERPR_PORTAL_TYPE_INJECTED` set just before the page's init-config script. The DERPR layer (engine routing, persona models, OAI endpoint auto-connect) enables itself based on that injected portal type.

### `src/interfaces/_persona_patch.py` -- persona PATCH helpers
`apply_patch_fields` / `apply_persona_patch_body` plus the known-keys derivation. Centralises the kobold-extras field mapping and the known-key list so both `PATCH /api/v1/persona/{name}` and `POST /api/v1/personas` apply persona edits through one chokepoint (unknown keys are surfaced as `rejected`/`unknown_fields` by the caller). Driven by the `persona_fields.py` registry.

### `src/agents/` -- Agent Framework

**`base.py` -- Agent (ABC)**
Abstract base for autonomous background workers. Not user-interactive — poll external systems on a schedule and act independently.
- Lifecycle: `start()` → `_on_start()` → `[loop: deploy() every interval]` → `stop()`
- `deploy()`: Abstract — subclass implements one work cycle
- `_build_llm_context()`: Minimal context (user prompt + optional action history injection)
- `_get_action_history_message()`: Injects recent actions into LLM context if `action_history_limit > 0`
- Auto-loads system personas from file on init (agents invoke system personas for read-only analysis)
- Config: `schedule` (dict, e.g. `{"interval": 30}`), `action_history_limit` (int), `agent_name` (str)

Trajectory-logging contract (DP-116a) — root + children written to `Agent_Actions`:
- `_log_task_root(action_type, trigger_context, action_payload, contexts, outcome="pending")`: opens a root row, JSON-serialises + ASCII-safe-truncates `action_payload`, attaches `Agent_Action_Contexts` rows; returns the root `action_id`.
- `_log_step(parent_id, action_type, action_payload, outcome, outcome_payload, contexts)`: child row under a root.
- `_add_contexts(action_id, [(type, value), ...])`: idempotent context-tag insert (drops null/empty).
- `_finalize_action(action_id, outcome, outcome_payload)`: terminal update on a root.
- `_serialize_payload(data)` / `_truncate_ascii(text, max_len=None)`: dict→JSON, ASCII-only (`encode("ascii","replace")`), capped at `MAX_PAYLOAD_CHARS` (4000) unless `max_len` overrides. ASCII-only is defensive against Hindsight's utf-8→latin-1 mangling on some endpoints.

Hindsight bridging (DP-116b) — fire-and-forget bridge of a finalized series into Hindsight:
- `_format_action_series_prose(parent, steps, contexts)`: flattens a root + children + context tags into dense k:v prose (no JSON braces, no nulls). Capped at **16000** chars (Hindsight chunks at 10k internally — a belt on pathological loops, never expected to fire when log-time ref discipline holds).
- `_retain_action_series(action_id)`: called by subclasses after `_finalize_action` on a root. Re-reads the series from `Agent_Actions` (idempotent across restarts), formats prose, enqueues a `retain_experience` POST with stable `document_id=f"agent_action:{action_id}"` (`_action_document_id`, prefix `AGENT_HISTORY_DOC_PREFIX`) + `content_override=<prose>`. Fire-and-forget through `HindsightBackend`'s queue; SQLite-shape backends raise `NotImplementedError`, which is swallowed — bridging is Hindsight-only and opportunistic.
- Class attrs `experience_bank` / `experience_persona` select the destination bank (default: `agent_name`). DispatchAgent points both at `DISPATCH_PERSONA_NAME` so dispatch series mingle into the `dispatch_analyst` persona bank; ReminderAgent + SqliteConsolidator default to their `agent_name`.

**`agent_manager.py` -- AgentManager**
Central registry and lifecycle controller.
- `register(name, AgentClass, default_config)`: Register agent blueprint
- `start_agent() / stop_agent() / restart_agent()`: Lifecycle control (creates asyncio tasks)
- `auto_start()`: Starts agents where config has `auto_start: true`
- Convention-based DI (`_di_kwargs`): inspects agent `__init__` signature, injects `chat_system`, optionally `zammad_client`, `notification_router`, `agent_config`, plus **any registered single-shot inference agent whose name matches a param** (DP-294 — e.g. `ZammadBot(content_classifier=…)`; the injected instance is `get_inference_agent`'s cached singleton). Shared by scheduled (`_build_agent_instance`) and inference (`get_inference_agent`) paths.
- `register_inference_agent(name, cls)` / `get_inference_agent(name)`: single-shot inference agents (DP-292) — DI-built on first lookup, cached, never launched as a task. `content_classifier` + `date_tagger` register here.
- Config merging: agents.json < registration defaults < runtime overrides
- `notification_router`: Lazy-set after construction

**`dispatch_agent.py` -- DispatchAgent**
Routes triaged tickets to notifications. Pipeline per ticket:
1. Fetch ticket + triage note from Zammad
2. LLM call to `dispatch_analyst` persona → priority, summary, reasoning (JSON)
3. Route notification via `NotificationRouter` (channel/recipient from agents.json config, not LLM-chosen)
4. Tag ticket as dispatched in Zammad
5. Log action outcome

Trajectory logging (DP-116a): each `_dispatch_ticket()` call writes one `dispatch` root row plus child rows for `tool:zammad.get_ticket`, `tool:zammad.get_ticket_articles`, `llm_step` (dispatch_analyst), `tool:notification.send`, `tool:zammad.add_tag`. Root `action_payload` carries `{ticket_id}`, `outcome_payload` the dispatch result; contexts `ticket_id` + `persona`. On finalize the series is bridged to Hindsight via `_retain_action_series` (into the `dispatch_analyst` bank).

**`reminder_agent.py` -- ReminderAgent**
Scheduled open-ticket nudge agent (shipped — registered in `main.py` when Zammad is available). Polls for tickets needing follow-up and routes a reminder notification via `NotificationRouter`. Uses the same DP-116a/b trajectory logging: `_log_task_root` per cycle + `_retain_action_series` on completion (bridges into its own `reminder` bank; `experience_bank` unset → defaults to `agent_name`).

**`zammad_bot.py` -- ZammadBot**
Multi-stage AI triage pipeline for new, untagged tickets. Uses 4 system personas:
1. `triage_scout` → extract search keywords
2. Search Zammad for related closed tickets (global + per-user)
3. `triage_filter` → relevance scoring of historical tickets
4. `triage_summarizer` → compress long ticket bodies (adaptive, only if context exceeds limit)
5. `triage_analyst` → full analysis with context → internal note posted to ticket
Tags ticket as triaged. No tools used — all LLM calls are read-only.
Content is classified first (DP-288) via `self.classifier` — a `ContentClassifier` single-shot inference agent injected by AgentManager convention-DI (DP-294), not constructed ad-hoc; flagged content is quarantined before it reaches any triage persona prompt.

**`managr_agent.py` -- ManagrAgent** (`agent_name="managr"`, DP-280/282/290)
Autonomous Zammad ticket-triage manager. Per cycle: builds a board snapshot, fans it out to read-only analyst personas for briefs, then a planner persona (`MANAGR_PLANNER_NAME`) produces the Manager's Report. When `proposals_enabled`, a second planner call (`tools=[submit_proposals]`) emits proposed writes into the `src/proposals/` queue instead of writing directly (see that section) — human approves via the portal. DP-290 also gives it reflective dispositions: it can reaffirm/revise/withdraw its own still-pending proposals from a prior cycle. Respects the DP-288 quarantine in code: tickets carrying a `QUARANTINE_TAGS` tag are excluded from the detail tier (title withheld, flagged as reported/suspected phishing) so their content never reaches a planner prompt — managr does not run the classifier itself; `ZammadBot` applies the tags upstream.

**`sqlite_consolidator.py` -- SqliteConsolidator** (formerly `MemoryAgent`; `agent_name="memory"`)
Batch agent that segments conversations by topic, extracts observations via LLM, and stores embedded summaries. **Registered only when `SEMANTIC_BACKEND=="sqlite"`** — the Hindsight backend drives consolidation upstream, and registering this agent under it would crash `deploy()` on the first cycle (legacy SQL ops raise `NotImplementedError`). Two-phase pipeline per channel:

**Phase 1 — Embed (loop until no unembedded messages):**
1. `get_unembedded_messages()` fetches msgs not yet in Message_Embeddings
2. `_chunk_messages()` splits into token-aware batches (item cap + token cap)
3. `EmbeddingService.encode()` batch API call
4. INSERT into `Message_Embeddings` + `vec_Message_Embeddings`

**Phase 2 — Segment + Summarize (loop until clear):**
1. `get_unsegmented_embedded_messages()` fetches embedded msgs with `parent_summary_id IS NULL`
2. `_segment_by_similarity()` groups msgs by centroid-based cosine similarity (threshold from config, default 0.80)
   - Seeds centroid from tail of previous segment for continuity
   - Min segment size configurable (default **1**)
   - Bridging (two mechanisms):
     a. Explicit: msg has `reply_to_id` pointing to a message already in the current segment
     b. Heuristic: fallback for pre-`reply_to_id` data — single user msg immediately followed by assistant msg
3. `_summarize_segment()` sends transcript to LLM via `memory_summarizer` system persona
   - Token guardrail: counts tokens via Google SDK (`count_tokens`), skips segment if > `RATE_LIMIT_GEMMA_4_TPR * 0.95`
   - Pre-processes: strips vertexai grounding redirect URLs from content
   - LLM calls `submit_memory_summary` tool with `observations[]` + `keywords[]` + `outlier_ids[]`
   - Outlier msgs stay `parent_summary_id=NULL` for re-queueing in next batch
   - Fallback: parses plain text if model doesn't use tool call
4. Stores segment + summary + vec embedding in DB, sets `parent_summary_id` on processed msgs

Config in `agents.json`: similarity_threshold, min_segment_size, batch_size (default **100**, capped at 100), allowed_channels, embedding_provider
Rate limiting: shared `GLOBAL_EMBEDDING_LIMITER` (AsyncLimiter) with MemoryConsolidator to prevent double-spending embedding quota

**`agent_service.py` -- AgentServiceIntegration**
Plugs agent tools into ChatSystem's service binding system. Personas with `service_bindings: ["agents"]` gain access to agent management tools.

**Agents vs Personas:**
- Agents are autonomous background workers — no user interaction, run on intervals
- Personas are conversational — respond to user messages, can use tools including agent management tools
- System personas are read-only LLM configs used by agents for analysis (no tools)
- Agents do NOT spawn other agents; no delegation chains currently

### `src/tools/agent_tool_handler.py` -- AgentToolHandler
Four tools gated behind `service_bindings: ["agents"]`:
- `get_agent_status` (read): Running state, deploy counts, errors for one or all agents
- `get_agent_history` (read): Recent action log with optional ticket_id/customer filters
- `manage_agent` (write): Start/stop/restart — parked for audit like any write tool (regardless of execution mode)
- `lookup_agent_history` (read, DP-116b): Dereference a single action series by `action_id` — returns the parent row + child steps + context tags. Used to recover the full trajectory after a Hindsight recall hit surfaces `action_id:<n>` from the bridged experience.

### `src/clients/notification.py` -- Notification System
- `Notifier` (ABC): `async send(recipient, subject, body) → bool`
- `DiscordNotifier`: DM via Discord bot
- `DiscordChannelNotifier`: posts to a Discord channel (registered as channel `discord_channel` in `main.py`)
- `ZammadNotifier`: Internal note on ticket
- `LogNotifier`: Fallback, logs to stdout
- `NotificationRouter`: Routes by channel name, falls back to LogNotifier. (Voice's `WebAlarmNotifier` registers the `web` channel — see `src/voice/`.)

### `src/app_manager.py` -- AppManager
Top-level lifecycle coordinator. Starts `agent_manager.auto_start()`, launches interface tasks (Discord, Gmail), blocks until every task finishes or shutdown is requested.

**Shutdown protocol (DP-304).** `start()` waits on `gather(*tasks, return_exceptions=True)` *and* a `_shutdown_requested` event, whichever comes first — `return_exceptions` so a crashing one-shot (`model_update` / `fixr_orphan_notify`) is logged rather than tearing down the interfaces with it. `request_shutdown()` is what SIGINT/SIGTERM reach, via `main._install_shutdown_handlers`; without it the only exit is `asyncio.run()`'s teardown, which cancels every task *before* `start()`'s `finally` runs, making the drain unreachable.

`shutdown()` is idempotent (`_shutdown_done`) and runs three phases:
1. **Signal, don't wait** — `agent_manager.signal_stop_all()` (agents share `MemoryManager._lock` with the consolidator, so they must stop *before* the drain, not after it), then each task's `stop=` callback. A `stop` may be sync or async; an awaitable return is awaited (`discord.py`'s `Client.close()` is `async def`).
2. **Cancel the uncooperative immediately** — a task registered without `stop=` was never signalled, so there is nothing to drain; waiting out the window would turn a ~0s shutdown into a 30s one.
3. **Drain cooperative tasks concurrently**, each against its own budget, then cancel the overruns and bounded-await the unwind.

Tunables: `GRACEFUL_SHUTDOWN_SECONDS` (30.0, default drain budget; override per task with `register_task(..., drain_timeout=)` when one work item can outlast it — `MemoryConsolidator.DRAIN_TIMEOUT_SECONDS` is 300.0 because its unit of work is a 31B summarizer call) and `CANCEL_GRACE_SECONDS` (5.0, bound on the post-cancel unwind; cancellation is a request, not a guarantee). Task names must be unique — they key the stop callbacks and drain budgets. A `drain_timeout` passed without a `stop=` callback is warned about. See decision `2026-07-30-interval-loop-shutdown-contracts` for drain-based vs cancel-based loop ownership.

### `src/clients/`
- `service_integration.py` -- ServiceIntegration abstract base: register_tools, resolve_context, get_tracking_id, prepare_tool_args, on_tool_result, on_message, get_system_messages
- `zammad_client.py` -- ZammadClient: wraps Zammad REST API (HTTP layer)
- `zammad_service.py` -- ZammadIntegration(ServiceIntegration): Zammad-specific tool registration, context resolution, user-facing ticket number ↔ internal ID translation
- The Zammad integration demonstrates the target pattern: separate client (HTTP) from service (tool registration + context). Discord and Gmail should eventually be split the same way (see pending interface refactor decision).

### `src/utils/`
- `google_utils.py` -- `process_grounding_metadata()`: extracts citations and sources from Google grounding responses, inserts inline citation markers
- `message_utils.py` -- `cleanse_message_for_history()`: strips citation markup and source blocks from messages before storing in history. `resolve_redirect_url()`: follows redirects with 429 retry for URL resolution
- `model_utils.py` -- `get_model_prefix()`: maps model names to family prefixes for routing and rate limiting. Model list refresh functions for OpenAI/Google/Anthropic APIs. **Model list — single source of truth is `chat_system.models_available`** (a snapshot taken at startup via `get_model_list()`, refreshed by the `update_models` command). Both consumers read it: the `what models` command, and the web UI model dropdown (`GET /api/v1/models/list` in both kobold adapters → flattens `chat_system.models_available`). The dropdown used to re-read the cache file via `get_model_list()` per request — unified to `models_available` on 2026-06-02 (DP-127) so it can't drift from `what models`. Underlying cache: `data/personas.json` → `"models"`. `get_model_list(update=True)` is the only path that hits provider APIs (slow) and rewrites the cache. **`STATIC_MODELS`** (`agy-flash`, `local`) are code-known, non-API models merged in on *both* the update and cached-read paths so they're always selectable without an API refresh — fixed 2026-06-01 (DP-127); previously they only existed on the update path and a pre-agy cache hid them.

### `src/personas/`
- `store.py` -- Persona JSON file I/O (moved from `utils/save_utils.py`, DP-203): load/save personas and the model catalog to disk. Handles default-persona auto-seeding, system-persona loading, and DP-128 quarantine-on-load validation via `src/tools/composition.py` (DP-204: the LOADER imports `tools/`, persona never does). Fields are read back with old-config-safe `.get(..., default)` defaults so an older `personas.json` still loads.

### `src/security/` -- Credential vault + egress scrubber (DP-225)
- `vault.py` — `CredentialVault`: the single inventory of machine secrets. Resolves credential refs (`KNOWN_REFS`: OpenAI/Anthropic/Google/Zammad API keys) via a pluggable source (default `os.environ.get`). `TextEngine` resolves all provider API keys through `get_vault()`. Process-global via `get_vault()`/`reset_vault()`.
- `scrubber.py` — `SecretScrubber`: process-global egress redactor. Registered exact secret values redact to `[REDACTED:<ref>]` (longest-first); a regex fallback catches unregistered secret *shapes* (`sk-ant-…`, `sk-…`, `Token token=…`, `Bearer …`) → `[REDACTED:pattern]`, skipped on strings > `MAX_PATTERN_SCAN_LEN` (100k) to avoid corrupting cached image blobs.
- Enforced at egress seams: `turn_persistence.store_api_request` (API-payload caching), `tool_loop` (audit_info + tool-result strings), `engine`, `zammad_client`.
- Startup wiring: `bootstrap.register_credentials()` calls `get_vault().register_into(get_scrubber())` from within `create_chat_system`.

### `src/self_edit/` -- "fixr" self-improvement supervisor (DP-227)
Event-driven dispatcher that sits ABOVE the engine. One dispatch = one bug = one `git worktree` (via `clone_manager.create_worktree`, off a pristine base clone; DP-314 also links the ONE shared notes clone into the worktree as `memory/`, and `remove_worktree` drops that link *before* removal so teardown never recurses into shared state) + one detached `claude --output-format stream-json` coding-agent subprocess. `dispatcher.py` tails the agent's raw stream-json log through a per-agent bridge task (never the live pipe), normalizes lines to common-schema `AgentEvent`s (`events.py`), and on a `{question,done,error}` wake event calls `on_wake`, which `FixrIntegration` (`integration.py`) wires to `chat_system.generate_response(CC_FIXR_PERSONA, …)`. `registry.py` holds `AgentRegistry`/`AgentRecord`; status persists through an optional `AgentStore` SQLite write-through (`store.py`, DP-233; `CC_FIXR_REGISTRY_DB`) so RUNNING/WAITING agents are reloaded as ORPHANED on restart, and `AgentRecord.archived` (DP-237) backs soft-archive + the `prune_agents` reaper. `FixrIntegration` is a `ServiceIntegration`; personas opt in via `service_bindings: ["fixr"]`. Tools (`fixr_tools.py`, **6**): `dispatch_fix` (`is_write:True` — the one always-gated write, parked for confirmation before the agent starts), `inspect_agents`, `answer_agent`, `kill_agent`, `prune_agents`, `send_discord` (the other five are `is_write:False`/ungated so the woken supervisor can coordinate, prune/reap, and report without a confirmation prompt per event). Approval at two boundaries: ConfirmationManager gates `dispatch_fix`, and a human merges the PR the agent opens — this module never merges or pushes. Config knobs in `global_config`: `CC_FIXR_MODEL_ARG`, `CC_FIXR_CLONE_DIR`, `CC_FIXR_PERSONA`, `CC_FIXR_CHANNEL`, `CC_FIXR_DISCORD_CHANNEL`, `CC_FIXR_REGISTRY_DB`.

### `src/voice/` -- Voice command subsystem (DP-238)
Browser/phone push-to-talk voice commands driving the text LLM. `VoiceIntegration` (`integration.py`) is a `ServiceIntegration` (name `"voice"`) that registers `VOICE_TOOLS` (`src/tools/tool_defs/voice.py` — `set_timer` / `list_timers` / `cancel_timer`, `service_binding: "voice"`); the text LLM owns timers via these tools (no separate voice model reasons over them). Registered in `main.py` at step 7.2. Pipeline: a complete utterance → Moonshine STT (CPU, `MoonshineTranscriber` in `transcriber.py`, lazy `moonshine_onnx` import) → intent → timer/alarm. The live capture source is the browser/phone push-to-talk web endpoint (`web.py`, mounted on the engine adapter's FastAPI app via `attach_web`, gated by `VOICE_WEB_ENABLED`). The Discord voice-receive path (`attach_discord`, `capture.py`) is dead (Discord DAVE E2EE — Opus never decodes) and short-circuits when invoked. Personas opt in via `service_bindings: ["voice"]`.
- Components: `transcriber.py` (`Transcriber` ABC + `MoonshineTranscriber`, Moonshine `base` on CPU; `warmup()` pre-warms to kill cold-start), `vad.py` (`VAD` ABC + `EnergyVAD`), `intent.py` (`IntentRouter` ABC + `KeywordTimerRouter`, deliberately OFF the product path — the text LLM owns timers), `pipeline.py` / `capture.py` / `resample.py` / `types.py`.
- `timer.py` — `TimerService` (in-memory). `alarm_bus.py` — `AlarmBus` (pub/sub) + `WebAlarmNotifier` (a `Notifier` on the `web` channel) so a web-set timer fires back in the portal over the SSE alarm stream.
- `web.py` routes on the adapter app: `GET /voice` (standalone PTT page), `POST /voice/utterance`, `POST /voice/transcribe` (STT-only), `WS /voice/stream` (always-listening per-connection `DictationStream`, silence-VAD endpointed, LAN-trust no auth), `GET /voice/alarms` (SSE).
- No DB writes (timers/alarms are process memory). Default-off via `VOICE_WEB_ENABLED` / `VOICE_*`; `useful-moonshine-onnx` is lazy-imported. Endpointing is silence-VAD-only today (open problem: half-sentence cuts vs latency).

### `src/proxmox/` -- Proxmox management subsystem (DP-262)
SSH-driven node/guest power ops + koboldcpp model swap on the GPU container's :5001 endpoint. Personas opt in via `service_bindings: ["proxmox"]`. The persona holding that binding is `hypr` (DP-327), which ships **unseeded** at `config/optional_personas/hypr.json` — an operator adds it to a specific instance's `data/personas.json` by hand. It is kept out of `default_personas.json` on purpose: `hypr` is the only persona exposing node power ops, and a park is approved by whoever raised it, so persona reachability *is* the authz boundary — seeding it would put that surface on every deploy. **DP-330 made that boundary enforceable rather than merely asserted**: `hypr` ships with an empty `origin_allowlist` (the guild id is the operator's and this repo is public) and the operator sets it on their own instance to pin `hypr` to one Discord guild. Accepted trade-off: an allowlisted `hypr` is Discord-only, so its parks can only be approved in Discord — letting the portal back in needs a transport-aware branch (portal auth is a `DERPR_CONTROL_TOKEN`, not a guild id), **not** a wildcard entry, which would grant every transport.
- `integration.py` — `ProxmoxIntegration(ServiceIntegration)` (name `"proxmox"`), registration-only. **Always** registers (even when `PVE_TOOLS_ENABLED` is false) so the startup-wiring contract holds; the handler short-circuits disabled calls with a clear error instead of attempting SSH.
- `handler.py` — `ProxmoxToolHandler` registers 8 tools; owns a config-driven `SSHRunner`.
- `ssh.py` — `SSHRunner` (config-driven host/user/key/timeout) runs remote commands as an **argv list** (never a shell string) via `asyncio.create_subprocess_exec("ssh", "-i", key, ...)`; `BatchMode=yes`, `ConnectTimeout`, hard `wait_for` timeout. `_reject_bad_args` refuses any arg containing shell metacharacters (defense-in-depth; only numeric vmids + config-pinned unit names ever pass). Transport failure/timeout → `SSHError`; a non-zero remote exit is returned, not raised. **`run_node_command(runner, argv, *, exit_label)` is the single gate in front of the node** and every tool that reaches the box goes through it: it owns the `PVE_TOOLS_ENABLED` check and the in-flight cap, maps a transport failure or a non-zero exit to a result dict, and lets the caller name what exited (`"remote command"` / `"node script"`). `MAX_INFLIGHT_SSH` + `inflight_semaphore()` live here too — the semaphore is held in a `WeakKeyDictionary` **keyed by running event loop**, not as a module global, because an `asyncio.Semaphore` binds to the first loop that awaits it and raises on a second.
- vmids are validated as digits; guest `kind` maps `ct→pct` / `vm→qm`. `set_active_model` disables every other **discovered** koboldcpp unit that binds `:5001` then `enable --now` the target (only one may hold the port). All executed on `PVE_MODEL_HOST_VMID` via `pct exec`. **It returns at fork, not at readiness (DP-353):** a `Type=simple` unit is active the moment koboldcpp is spawned, minutes before it has read the gguf into VRAM and bound `:5001`, so the result carries `state: "loading"`, `size_bytes` from the tier inventory and a note pointing at `gpu_status` — `list_models`' `state` is `systemctl is-active` and cannot distinguish loading from serving. Nothing observes the load; making the resume automatic would mean giving the swap a `node_job` deferral the way `install_model` and `_promote_model` have one.
- `_run` is one call into `ssh.run_node_command`. The cap it passes through exists because every call is a fresh `ssh` process doing a full auth handshake (no `ControlMaster`), and the per-unit probes fan out over however many units the box happens to hold — a number DP-332 deliberately stopped bounding in config. Past sshd's default `MaxStartups` (10:30:100) connections start being dropped at random, so the cap sits at the one choke point every call passes through rather than at each fan-out site. **DP-348 moved that choke point from the handler into `ssh.py`**: both the cap and the `PVE_TOOLS_ENABLED` check are properties of *the key and the node's sshd*, not of one handler, and while they lived here the HuggingFace tools — which grew their own `_run` — were exempt from both. `install_model` shelled out to the box while `pve_status` on the same persona reported itself disabled, and HF connections were invisible to the cap that exists to stay under `MaxStartups`. The semaphore was also *per handler instance*, so two `ProxmoxToolHandler`s would not have shared it either. Pinned by `tests/integration/test_node_transport_gate.py`, which asserts the seam rather than either handler: `install_model` refuses with the transport off, both handlers report the same disabled state, the cap is one object, and no handler calls `SSHRunner.run` outside the gate.
- Config: `PVE_TOOLS_ENABLED` (default-off; disabled → tools return a clear error, no SSH), `PVE_SSH_HOST` / `PVE_SSH_USER` / `PVE_SSH_KEY` / `PVE_SSH_TIMEOUT`, `PVE_MODEL_HOST_VMID` (CT101). Tool defs in `src/tools/tool_defs/proxmox.py`.
- Tools in `src/tools/tool_defs/proxmox.py` (`PROXMOX_TOOLS`, 8): read — `pve_status`, `list_models`, `gpu_status` (`exfil_capable=False`, no args); write (parked for confirmation) — `reboot_node` (also `irreversible`), `reboot_guest`, `start_guest`, `stop_guest`, `set_active_model`. All carry `produces_untrusted: False`, `locality: "network"`, `sensitivity: "internal"`.
- **Every write here is `exfil_capable: False` as of DP-265** (was: only `set_active_model`, DP-263). Honest, not convenient: the arguments are vmids, guest names and model keys *discovered from the node*, and the value that crosses SSH is always the resolved one — `_resolve_guest` matches a name locally and sends digits, `set_active_model` sends the discovered unit. The flag became load-bearing when DP-265 put untrusted HuggingFace reads on the same persona: without it, `ToolPolicy` Rule 2 sees untrusted reads in the `huggingface` domain beside network writes in the `proxmox` domain and quarantines `hypr`. The alternative — an `explicit_overrides` entry — would disarm Rule 2 for that persona's whole toolset permanently, including for tools nobody has written yet. `pve_status`/`list_models` keep the default `True`, which is what sets `has_network_read` so Rule 1 still fires if a local write is ever added. A future proxmox tool that genuinely carries a model-authored string out to the node **must not copy the flag**; pinned by `tests/test_tool_policy.py::test_every_proxmox_write_is_now_exempt_and_says_why`.
- ⚠️ **The node-side forced-command wrapper is the second half of every tool here** (`services/pve/derpr-pve-wrapper`, DP-267, versioned in-repo as of DP-265). sshd runs it instead of whatever derpr asked for, so a command shape the handler emits and the wrapper does not admit fails **in production and only in production** — a developer's own node key is unrestricted, so a live smoke test from a workstation proves nothing about the deployed path. DP-332 shipped `list_models` and `gpu_status` with three new shapes (`systemctl list-unit-files`, `ls /sys/class/drm`, `cat …/mem_info_vram_*`) and no wrapper update; unit tests, mypy and a live read-only smoke test all passed while both tools were dead on the deployed container. **Change an argv, change the wrapper in the same commit**, and verify with the container's key. `services/pve/README.md` has the deploy + verify steps.
- **Model-availability guard (DP-264):** `list_models` omits units whose gguf isn't on disk (a missing file can't be loaded, and enabling it would take :5001 down); `set_active_model` refuses the swap — leaving the current model running — unless the target's model file is present, so it never disables the live model to enable one that can't start (`_model_present` / `_unit_spec` in `handler.py`). `_unit_spec` parses one unit's `ExecStart` into `{model, port, contextsize, quantkv, readable}`; `_flag_value` accepts both `--flag value` and `--flag=value`, because systemd echoes `ExecStart` back verbatim and DP-332 is an invitation to hand-write units, and `_flag_int` coerces or drops. **`contextsize`/`quantkv` are published on each `list_models` row (DP-344)** — they are already in the tokens this call parses, so they cost no extra round trip, and they are the box's own *empirical* evidence about what the card holds: a unit that has been serving `:5001` at a given context has demonstrated that context fits, which outranks any figure derived from a gguf header. They are omitted rather than sent as null when the flag is absent, so "runs 163840" and "does not say" stay distinct.
- **Discovery-driven models (DP-332):** `_discover_units(vmid)` enumerates `systemctl list-unit-files --type=service` on the GPU CT and keeps whatever matches `_UNIT_RE` = `koboldcpp-([A-Za-z0-9._-]+)\.service`; the capture group is the friendly name. This replaced `PVE_MODEL_UNITS`, a config map that asserted what the box contained and drifted silently in both directions — DP-329 found the unit actually serving `:5001` unmapped, so `list_models` called every model inactive and no swap could succeed, with nothing erroring. It is also the prerequisite for DP-265: a freshly installed unit is reachable by `set_active_model` with no config edit and no redeploy. Four properties are load-bearing:
  - **The `koboldcpp-` anchoring happens in Python, not as a `'koboldcpp-*.service'` argument.** `ssh._reject_bad_args` forbids `*`, and pushing the glob remote would mean trusting the far-side shell to expand it as intended. `_UNIT_RE` is the *outer* bound on the set `set_active_model` disables — this is the one place discovery is *more* dangerous than the map was, since a loose match would take down something unrelated on the CT. Pinned by `test_set_active_model_disables_only_koboldcpp_units`, which seeds decoys (`nginx.service`, a bare `koboldcpp.service`, a `koboldcppx-` near-miss) into the default mocked listing.
  - **Discovered unit names are remote values that then travel back over SSH**, which the config map never did. `_UNIT_RE`'s character class is deliberately narrower than `ssh._FORBIDDEN` permits. `set_active_model`'s `name` argument stays a pure lookup key and never becomes an argv element — that is what keeps its `exfil_capable: False` claim true.
  - **The disable set is bounded by port, not by name (`_port_contenders`).** `_UNIT_RE` excludes names that aren't koboldcpp units at all, which was never the dangerous case: a *genuine* `koboldcpp-<name>.service` serving something else on another port — an embedding or draft server, a helper — matches the pattern perfectly, is not shown by `list_models`, and would be `disable --now`'d by every swap. The config map couldn't name one; discovery can. So `_unit_spec` parses each unit's `--port` (defaulting to koboldcpp's own `5001`, so "no `--port`" means *contends*), `list_models` publishes only the `:5001` units, and `set_active_model` disables only those — the shown set and the acted-on set stop diverging. A unit whose `ExecStart` can't be read counts as contending and is reported in the result's `warnings`: it may be the one holding `:5001`, and skipping it is how a swap "succeeds" while the old model keeps answering.
  - **Every disable's result is checked, and a failure aborts before the enable.** `systemctl disable --now` over `pct exec` can sit on a D-Bus job past `PVE_SSH_TIMEOUT` and get SIGTERM'd; the unit keeps `:5001`, and `enable --now` on a `Type=simple` target *still* exits 0, because "started" is not "bound". Discarding the disable results therefore reproduces the exact DP-329 silent failure this ticket exists to remove. The error says that units stopped before the failure stay stopped, so `:5001` may be serving nothing until a swap succeeds — an honest degraded state beats a false `active_model`.
- **`gpu_status` (DP-332):** reads `mem_info_vram_total` / `mem_info_vram_used` from the GPU CT's sysfs, returning total/used/free MiB per card. The card index is discovered from `ls /sys/class/drm` filtered by `_CARD_RE` (`card\d+`, so connector nodes like `card1-DP-1` and `renderD128` are excluded) — the live box exposes `card1`, so a hardcoded `card0` reads a path that does not exist. Rationale: DP-265 wants a VRAM budget before choosing `--contextsize`; the numbers are site-specific and must never be baked in (DP-328). **DP-337 moved the budget *formula* out of the persona prompt and into this tool layer** — the equation onto `install_model.contextsize` (the one argument it constrains) and the GTT-spill consequence onto this description; see `_kv_budget_note` below for the arithmetic itself. **There are two formulas, and the prompt has to keep them apart.** Growing the context of the model that is *already running* fits into free MiB. A swap does not: stopping the current unit releases everything it holds, so a `set_active_model` candidate is sized against total minus what stays resident. Sizing a swap against free MiB reads the live box (32624 total / 28601 used / 4022 free) as "nothing over 4 GiB fits" and refuses every swap there is — the failure mode the `hypr` prompt and the `gpu_status` tool description now both spell out. Free MiB is floored from the byte difference rather than from `total_mib - used_mib`, so the three can disagree by 1 MiB — overstating free VRAM is the direction that picks too large a context. Never raises: a dead CT, a missing sysfs path, or a non-amdgpu card comes back as an error dict or an entry in `errors`.
- **Guest inventory + name addressing (DP-327):** `_parse_table` turns a Proxmox fixed-width CLI table into row dicts keyed by its own lowercased header tokens, slicing at the header's column offsets. Header-driven rather than whitespace-split because `pct list`'s `Lock` column is usually **blank** — splitting on whitespace shifts `Name` into `Lock`'s slot — and because `pct list` and `qm list` disagree on both column set and order. The first field is anchored at column 0 so a right-aligned `VMID` wider than its header (`qm list`) is captured rather than truncated. Rows whose `vmid` isn't numeric are dropped, not guessed at.
  - `_guest_inventory()` gathers `pct list` + `qm list` concurrently into `{vmid, name, kind, status, lock}` dicts; one listing failing is reported in `errors` rather than aborting the other. `pve_status` returns this as `guests` **alongside** the raw text (which still carries columns the parse drops: mem, bootdisk, pid) — that structured field is what makes `pve_status` an infra-topology audit rather than two blobs to re-read each turn.
  - `_resolve_guest(vmid, name, kind)` accepts either address form. **The name is matched locally and never becomes an argv element** — only the resolved digits reach `SSHRunner`, which is precisely why `ssh.py`'s metacharacter guard remains a second line of defence rather than the only one. Pinned by `tests/proxmox/test_guest_lookup.py::test_name_never_crosses_the_ssh_boundary`.
  - `kind` is optional with a name (the lookup supplies it) and **required** with a bare `vmid` — an id alone doesn't say which CLI owns it, and guessing wrong on a power-off is not worth risking. Unknown names are refused with the list of names that exist; a name held by both a CT and a VM is refused as ambiguous until `kind` disambiguates.
  - Resolution runs at **execution** — after the park is approved — and the result carries a `target` block echoing the `vmid`/`kind`/`name` acted on, so the audit row records the resolved guest rather than only the argument the model typed. If **both** `name` and `vmid` are supplied they are cross-checked and a disagreement is refused: acting on the `vmid` while echoing the caller's `name` would put a guest we did not touch into the audit row and into the approval prompt the human read. A failed listing is reported as *not confirmable* rather than as "no such guest" — unknown and absent are different answers on a power path. `_resolve_guest` also short-circuits when `PVE_TOOLS_ENABLED` is false, so the disabled state reads as disabled instead of as an empty node.
  - The `_GUEST_PARAMS` schema marks nothing `required`; "one of name or vmid" is enforced in the handler because providers vary in whether they honour `anyOf`/`oneOf` in a function schema, and a constraint the provider silently drops is worse than none — it reads as enforced.

### `src/huggingface/` -- HuggingFace model provisioning (DP-265)
Find a gguf on the Hub and provision it onto the pve node, so it *becomes* a valid `set_active_model` target. Personas opt in via `service_bindings: ["huggingface"]`; the only one holding it is `hypr`, which also holds `proxmox` — deliberately the same blast radius rather than a new one. Sits **above** `src.proxmox` in the import-linter layer order because it reuses that package's `SSHRunner` instead of opening a second transport to the same node.
- `client.py` — `HFClient`: two read-only Hub calls (`/api/models?filter=gguf` search, `/api/models/<repo>/tree/<rev>` file listing), one `aiohttp` request each, no held session. `HFFile{path, size_bytes, sha256}`. `validate_repo_id`/`validate_file_path` gate every caller-supplied string *before* it is interpolated into a URL — percent-encoding a traversal yields a 404, and a 404 reads to a model as "try another spelling". `_as_gguf_file` prefers `lfs.size` over the top-level `size`: on a repo storing an uncommitted pointer the latter is ~135 bytes, and believing it would size the node's free-space precheck three orders of magnitude too small. An `lfs.oid` that isn't 64 hex (a git blob sha1) is dropped rather than passed through as a digest. `find_gguf_file` **refuses a file with no published sha256** — the digest is the only thing tying the bytes that land on the node to the bytes a human approved. Tree pagination follows `Link: rel="next"` up to `_MAX_TREE_PAGES`, so a sharded repo cannot decide how long a tool call runs.
- `handler.py` — `HuggingFaceToolHandler` registers 4 tools; owns an `HFClient` and shares the proxmox `SSHRunner`. Both injectable, so the whole feature tests with no network and no node. Its `_run` is one call into `proxmox.ssh.run_node_command` (DP-348) — it used to be a second, independent implementation of `ProxmoxToolHandler._run` that checked neither `PVE_TOOLS_ENABLED` nor the in-flight cap.
- `integration.py` — `HuggingFaceIntegration(ServiceIntegration)` (name `"huggingface"`), registration-only for tools, **always** registers so the startup-wiring contract holds. Since DP-343 it also owns the non-tool `completion_bridge` (below), which exists only when the composition root gives it a `ChatSystem`.
- `completion.py` — `JobCompletionBridge` (DP-343, rewritten onto DP-345): **resolves the `node_job` deferral parked under the finished job's id**, which resumes the turn that started the job. It does not create a turn: there is no synthetic user message and no `generate_response` call, because the job id IS the park token and the row it names already holds the conversation. See **The node pings when a job ends** below.
- Config: `HF_TOOLS_ENABLED` (default-off), `HF_API_BASE`, `HF_API_TOKEN` (gated repos only), `HF_HTTP_TIMEOUT`, `HF_SEARCH_LIMIT_MAX`. Transport settings are the proxmox ones — **and so is the transport switch**: `HF_TOOLS_ENABLED` turns these four tools on, `PVE_TOOLS_ENABLED` decides whether this instance may drive the node at all, and `install_model` / `install_status` need both. Two switches because they answer two questions (does the operator want the feature / may this deploy touch the box), and conflating them is how the node ended up reachable with the proxmox tools off. DP-343 adds `MODEL_JOB_CALLBACK_TOKEN`, `MODEL_JOB_CALLBACK_PATH` and `MODEL_JOB_ALERT_CHANNEL_ID` — **and deliberately nothing that names a conversation.** DP-343 originally carried `MODEL_JOB_WAKE_PERSONA`/`_CHANNEL`/`_USER` too; DP-345 deletes all three, because the deferral row keyed by the job id already carries the persona, the channel and the user of the turn that started the job, and a setting here could only ever disagree with it (see `src/deferral_kinds.py` above).
- Tools in `src/tools/tool_defs/huggingface.py` (`HUGGINGFACE_TOOLS`, 4): read — `hf_search`, `hf_files` (both `produces_untrusted: True`, `sensitivity: "public"`), `install_status` (`produces_untrusted: False`, explicit `domain: "proxmox"`, `exfil_capable: False`); write (parked) — `install_model` (explicit `domain: "huggingface"`).
- **The composition is the point.** Untrusted third-party reads sit beside node write access on one persona, so the invariant is *checked*, not assumed: the Hub reads and `install_model` share the `huggingface` egress domain, making `untrusted_read_domains == network_write_domains` — a same-origin closed loop that validates clean while leaving Rule 2 **armed** for anything added later. `install_model` is tagged `huggingface` even though derpr's own hop is SSH, because the payload it carries (the repo id) is what the node then fetches *from the Hub* — accurate tagging, not a dodge. `install_status` uses the same `capabilities["domain"]` seam in the opposite direction (`proxmox`), since its egress really is the node. This is the seam's first real user; it has existed unused since DP-128. Pinned by `tests/huggingface/test_hf_composition.py`, including a test that an added foreign-domain write still trips the rule.
- **What the model never decides.** (1) *What bytes land*: `install_model` reads the file's size and sha256 from the Hub itself and passes them to the node, which refuses on any mismatch. The enricher registered alongside the handler puts those same two values on the approval card, so a human approves a digest rather than a repo name — and an enricher failure returns loud `⚠️ UNVERIFIED` text rather than raising, because `ToolManager.enrich_audit_action` swallows exceptions into `None` and would render an ordinary-looking card that verified nothing. (2) *Where they land*: the node script owns the models directory and the unit template; no argument is a path. The Hub is re-read at execution, so a repo that changes the file between park and approval yields a different enforced digest — reported in the result rather than silently swapped.
- **`install_status` evaluates the KV budget rather than reciting it (DP-337).** `handler._kv_budget_note` turns the header's `n_layer` / `n_kv_head` / `head_dim` into bytes-per-token, the cache size at the installed `contextsize`, and the unit's total VRAM want (model buffer + KV + `_COMPUTE_BUFFER_MIB` 1010 + `_VRAM_MARGIN_MIB` 500). `bytes per element` is the constant ratio `_KV_BLOCK_BYTES 34 / _KV_BLOCK_ELEMS 32`, not a parameter, because `koboldcpp-model.service.in` hardcodes `--quantkv 1` and no tool exposes a knob for it — q8_0 stores a block of 32 quantised values in 34 bytes (32 int8 plus one f16 scale), so the cache is 1.0625 B/element. **DP-344 corrected this from `1`**, and the 6.25% shortfall had hidden because `n_layer` was simultaneously one too high on the model it was checked against: 17/16 is exactly 1.0625, so two errors cancelled to the right total for Qwen3.8-27B and to a wrong one for every other model. `n_layer` is now the count of layers that **cache** K/V, which on the hybrid architectures this host serves is a fraction of `block_count`. When the node determines the linear formula does not describe the model at all it sends `kv_shape_note` **instead of** the three numbers, and the note relays that reason and emits no arithmetic; a node older than DP-344 never sends it and degrades to the pre-existing "header did not publish" message. **The note is gated on `state == "done"`**: the node folds the header in only after the bytes verify, so an absent shape earlier means *not yet*, and reporting that as *unreadable* would be a worse answer than silence. Placement rationale is the general form of DP-335's root cause — a tool result arrives at the moment of need, costs nothing until called, versions with the code and is testable; a persona prompt is none of those, and here is hand-maintained on a docker volume so it does not even ship with the merge.
- **`install_status` re-publishes a whitelist**, not the node's document: `_STATUS_FIELDS` names every key and its type and truncates strings to 200 chars. That whitelist is what keeps the tool's `produces_untrusted: False` claim *enforced* — a future node-side change that started echoing an HTTP error body cannot quietly turn a trusted read into an injection surface.
- **The node side is the security boundary** (`services/pve/`, deployed by hand — see its README). One script, `/usr/local/sbin/derpr-model-install`, exposed as exactly one forced-command allowlist family (`install` and `status`; its `run` subcommand is invoked by systemd locally and is **not** admitted by sshd). derpr never gets `curl`, `systemctl`, or file writes as separate verbs. The script refuses before any bytes move on: an existing unit of that name, an existing file with a *different* sha256 (an identical one is reused), or insufficient free space (the larger of 2 GiB or 5%). ⚠️ **That last check guarded the wrong layer until DP-340.** It reads `df`, and it read it against `/srv/models` — a thin LV, where `df` reports the *volume's* free space and is structurally blind to the *pool* behind it running out. On 2026-08-21 it saw 75 GiB free over a pool with 14 GiB left, passed a 22 GB download, and put CT100 and CT101 into ext4 `emergency_ro` — taking zammad, the chatbot and hindsight's embedded postgres down together. Downloads now land on the **archive disk** (`/srv/archive/models`), an ordinary partition where `df` is the truth, so the pool is protected by not being written to rather than by a check. After download, a sha mismatch deletes the partial file and fails the job. Size and sha are **arguments**, not something the node re-derives — which also means the node needs no JSON parser, and a stock PVE node has no `jq`.
- **The unit lands disabled.** Installing does not put a model on `:5001` and does not disturb the running one; `set_active_model` is a separate call with its own approval. `contextsize` defaults to a deliberately small 8192 on the assumption a human tunes it against `gpu_status` first.
- **Long downloads run on the node, not in derpr.** `systemd-run --collect --unit=modelinstall-<job_id>` survives the SSH disconnect and derpr polls `install_status`. A background loop inside derpr was rejected: it re-opens the DP-304 interval-loop shutdown-contract problem for a job the node already supervises.
- **The node pings when a job ends (DP-343).** Detachment is what makes a multi-GB download survivable and is also why nothing in derpr is awake when it finishes: before this, a completed install waited for a human to ask again, and a cold-tier promotion — which `set_active_model` starts and does not wait for — stopped one step short of serving for the same reason. Both node scripts now fire `ping_derpr` from `write_status` on a terminal state (`done`/`failed`), POSTing to `MODEL_JOB_CALLBACK_PATH` (`/api/v1/model_job/complete`) on the engine adapter. The flow is `ping (job id only) → handler.job_status over SSH → ChatSystem.stream_resolve_deferral(job_id, kind="node_job") → NotificationRouter.send("discord_channel")`. Six properties are load-bearing:
  - **The ping is a doorbell, not a report.** The body is `{"job_id": ...}`; everything the persona is told comes from `job_status`, i.e. the same SSH read `install_status` uses, re-published through the same `_STATUS_FIELDS` whitelist. A forged or replayed ping therefore cannot assert an outcome — it costs one status read of a document that says what it already said. Sending the node's status payload instead would have made this an input path into a persona's turn. The job id in that body is also a park token now (see `confirmations.py`), and it cannot be spent as a human's verdict: `stream_resolve_deferral` refuses a token whose park is an `approval` and restores it.
  - **A third principal, with its own credential.** The route is authenticated by `MODEL_JOB_CALLBACK_TOKEN` and exempted from the DP-277 operator middleware exactly like the MCP bridge is — and only while a handler is wired, so an instance that never deployed the node half grows no surface. Handing the node `DERPR_CONTROL_TOKEN` would have made a bash script on the model host an operator, able to edit personas and approve its own parks.
  - **The turn that is resumed is the turn that asked, and nothing configures that.** `handler._install_model` and `proxmox/handler`'s cold-tier promotion both return through `declare_deferral(..., DEFERRAL_KIND_NODE_JOB, job_id)`, and the job id they hand the node *is* the park token — so a durable `Parked_Writes` row already holds the persona, channel and user of the turn that started the job. Not cosmetic: `hypr` is `CHANNEL_ISOLATED`, so "activate it when it lands, if I said so" only works if the resumed turn sees that channel's history; and a write the turn parks is keyed `(user_identifier, persona)` and rendered by `_post_pending_proposals` only where `ParkedWrite.channel` matches, so a resume under a synthetic user raises approval cards nobody can ever answer. DP-343 got the same outcome by *naming* those coordinates in config and entering the turn through `ChatSystem.generate_response`, which also persisted the synthetic wake text as a durable **user** row under the operator's real Discord id. Both the three env vars and that row are gone.
  - **The announcement is posted, not delegated.** The resumed turn's reply goes to `MODEL_JOB_ALERT_CHANNEL_ID` verbatim via the NotificationRouter — needed because this resume has nobody holding a stream open to read it, unlike an operator clicking approve. The alternative — a `send_discord`-style tool the persona may call — is fixr's shape and right when a human is present to notice silence; here nobody is, and the turn most worth hearing about is the failed one a model may judge self-explanatory.
  - **Nothing raises, and a repeat resolves once.** The only consumer of the HTTP status is a `curl` in a bash script whose job is already finished, so a failed turn is an error dict rather than a 500. The node retries its POST, and the retry is absorbed by the **durable claim in `take()`**: the second ping finds the park already claimed, settles nothing and returns quietly, so a lost reply does not re-run a turn (or re-park a swap). DP-343 did this with an in-process set of handled job ids, which was *weaker* in the case that matters — it was lost on restart, so a ping arriving after one resolved nothing at all rather than being deduplicated.
  - Node-side, the ping is **best effort by construction**: it fires after the job file is renamed into place (derpr answers by reading it), every path returns 0, and an unreachable derpr costs the announcement and nothing else. Covered as real bash in `tests/services/test_job_completion_ping.py`.
- **Hot/cold model tiering** (`services/pve/derpr-model-tier`, DP-340). Two tiers with the **cold one authoritative**: `/srv/models` on the SSD is what koboldcpp serves and holds a working set (~4-5 models, capped by `HOT_CAPACITY_BYTES`), while `/srv/archive/models` on the 1 TB archive HDD holds every gguf ever installed. `install_model` writes only to the archive; `set_active_model` promotes cold→hot on demand, evicting **least-recently-served** first, never a pinned model and never the one currently serving. Four things are load-bearing:
  - **Promotion is detached** (`systemd-run`, a job record polled through `install_status`) and returns `state: "promoting"` immediately. A 24 GB copy off a spinning disk is minutes, and a tool call held open that long is indistinguishable from a hung one — which an agent retries, racing two promotions for the same space (the DP-338 shape).
  - **Nothing is disabled while promoting.** `:5001` keeps serving until a second `set_active_model` swaps to the promoted file, so a failed promotion costs time and nothing else.
  - **Eviction never deletes a hot file without a verified archive copy** (present *and* sha-matching). That invariant is the only reason eviction is a cache operation rather than data loss.
  - **When pins leave no room the promotion fails and names them** rather than overruling one. A pin the system may override is not a pin. This is deliberately the *opposite* failure policy from the in-process LRU caps, which must always make room — recorded as a second LRU `by design` in `docs/capability_map.md`.
  - Free space is read as the **minimum of `df` and the thin pool's own free blocks** (`lvs data_percent`), for the reason in the paragraph above: on a thin LV, `df` alone is not a fact about whether a write can land.
  - **The handler degrades to pre-DP-340 behaviour when the node has no tier script** (`_tier_inventory` returns `{}`), because node artifacts and the container image are two independent deploys.
- `services/pve/gguf_header.py` (stdlib-only, deployed beside the installer) reads `n_layer` / `n_kv_head` / `head_dim` out of the downloaded gguf's header **and tensor index** and the installer folds them into the job status, so `KV_bytes_per_token = 2·n_layer·n_kv_head·head_dim·bytes_per_elem` can be *evaluated* rather than recited. It prefers a published `attention.key_length` over `embedding_length / head_count` — several architectures use a head dim that is not the quotient, and deriving it there understates the cache on exactly those models. **`n_layer` is counted off the tensor index — blocks carrying an `attn_k.weight` (or `attn_qkv.weight` for fused-QKV archs), less any block carrying `nextn.*` — and NOT from `block_count` (DP-344).** The two differ on every model this host serves: Qwen3.8-27B has 65 blocks of which 17 attend and 16 are cached (block 64 is its MTP draft layer, which koboldcpp only runs under the ROCm-only `--usemtp` that no unit here passes); Deckard-40B has 96 blocks and 24 cached. Using `block_count` overstated the cache 3.8-4x and produced a live answer of "32k is the maximum context" for a model that fits 262144. Validated to the byte against koboldcpp's own `llama_kv_cache: KV buffer size` on both. It also **refuses with a reason** (`kv_shape_note`) rather than estimating when the header says the cache is not linear in context: a per-layer `attention.head_count_kv` array (gemma4 publishes 16 on windowed layers, 4 on full-attention ones) or a published `attention.sliding_window`. That refusal required recording array values as markers instead of dropping them — an array that read back as absent made `head_count_kv or head_count` substitute gemma4's *query* head count, reporting ~2 MB/token. Unparseable input produces **no output and exit 0**: a header quirk must never fail an install whose bytes verified, and a partial shape yields nothing rather than a confident wrong number.

### `src/tools/mcp_client.py` + `mcp_integration.py` -- MCP client (DP-268)
Consume external MCP tool servers (streamable-HTTP transport). Discovered tools become ordinary derpr tools (`mcp__<server>__<tool>`, `service_binding: "mcp:<server>"`) — parking, taint, composition rules, and per-persona policy all apply with no MCP-specific downstream paths. Personas opt in per tool + bind `mcp:<server>` (no wildcard — see `dynamic` below).
- `mcp_client.py` — `MCPClientManager`, **owned by `main.py`** (voice precedent: `ServiceIntegration` is registration-only, so main.py owns the session lifecycle). Each server runs in a dedicated `_ServerConnection` task that owns the anyio transport contexts (must be entered/exited in one task); `_open_session` is the test seam. `start()` connects every enabled server from `MCP_SERVERS_FILE` (dead server logged + skipped, never breaks startup); `add_server` connects + discovers + registers the tools live and persists config **only on success** (rollback on failure; strict config load so a corrupt file is never clobbered); `remove_server` stops the session, unregisters defs + handlers, persists; `list_servers` reports url/enabled/connected/tools; `call_tool` invokes the server session. Every live (de)registration triggers `revalidate_persona_security` across all personas via `personas_provider`.
- **Translation** (`_translate_tool`): each discovered tool defaults to the most-restrictive metadata (`is_write: True`; `produces_untrusted`/`irreversible: True`, `locality: "network"`, `sensitivity: "pii"`), relaxable only by operator per-tool `tool_overrides` in the config file — server-provided annotations (`readOnlyHint`/`destructiveHint`/…) are logged as hints, never policy. Definitions carry **`dynamic: True`**, the marker that excludes them from `['*']` wildcard expansion in `ToolPolicy.filter_tools` (a new server can never silently widen or quarantine-cascade wildcard personas). Server name is validated, namespaced tool name capped at the 64-char provider limit, description capped at 1024 chars (prompt-injection surface).
- **Hot reload (phase 3):** a background maintenance loop reconnects dead servers and re-discovers a server's toolset when it signals `tools/list_changed` (the periodic tick is the fallback). Network work runs unlocked; registration swaps are all-or-nothing and a no-op when the toolset is unchanged (flap-safe). `MCP_RECONNECT_INTERVAL <= 0` disables the loop. While a server is down its tools stay registered and degrade to per-call `{"error": …}`, so persona policies stay stable across an outage.
- `mcp_integration.py` — `MCPIntegration(ServiceIntegration)` (name `"mcp"`), registration-only: attaches the `ToolManager` to the manager and registers the three management tools. **Always** registers (even when `MCP_ENABLED` is false) so the startup-wiring contract holds; disabled calls short-circuit with a clear error.
- Tools in `src/tools/tool_defs/mcp.py` (`MCP_TOOLS`, `service_binding: "mcp"`): `add_mcp_server` (`is_write` → parked; `url` is model-controlled egress, exfil-capable), `remove_mcp_server` (`is_write` → parked; `exfil_capable: False`), `list_mcp_servers` (read; `produces_untrusted: True`, `exfil_capable: False`).
- Live catalog: `ToolDefinitionRegistry` (`definitions.py`) holds discovered defs; `ToolManager.unregister` drops their handlers; discovered-tool handlers are closures over `MCPClientManager.call_tool`.
- Config (`global_config.py`): `MCP_ENABLED` (default off), `MCP_SERVERS_FILE` (default `DATA_DIR/mcp_servers.json`), `MCP_CONNECT_TIMEOUT` (30s), `MCP_CALL_TIMEOUT` (120s), `MCP_RECONNECT_INTERVAL` (60s). Dependency: `mcp` SDK.

### `src/tools/mcp_bridge.py` -- MCP **server** for dispatched subagents (DP-240)
Mirror of the MCP client above: derpr HOSTS an MCP server so a *capable* fixr subagent can call derpr tools. Mounted as a raw ASGI sub-app on the kobold adapter's FastAPI at `MCP_BRIDGE_PATH` (default `/mcp`); `StreamableHTTPSessionManager(stateless=True)`, whose task group is owned by the adapter's `_lifespan`.
- **Trust model (do not refactor away):** the server lives in the trusted derpr process; the subagent reaches it over HTTP across the sandbox. Deliberately NOT a stdio server spawned as a child of the agent's `claude` — a parent can subvert its own child, and the parent is the thing being gated. Capable dispatches keep `--dangerously-skip-permissions` (decided 2026-07-23), so **this module is the only authz boundary** — no defense-in-depth behind it.
- **Gating:** `_is_gated` = `is_write` OR `capabilities.irreversible` (independently sourced; fails closed on either). Gated → `propose_call` writes a `call_derpr_tool` proposal row and returns `{status: "parked_for_approval", proposal_id}` **without executing**; the tool description is annotated so the agent can plan around the park. Ungated → `runner.execute_ungated`. Dedup is safe: `create_proposal`'s ON CONFLICT clause is `WHERE ... ticket_number IS NOT NULL`, and agent calls carry no ticket, so each gets its own row.
- **Auth:** `BridgeTokenStore` mints one bearer token per capable dispatch (`resolve` compares constant-time across all live tokens). Verified in `handle_asgi` *before* the SDK is entered; the agent id is bound to a `ContextVar` the handlers read. `_handle_call` re-checks that binding as a fail-closed backstop — an unattributable call can't be gated or audited, so it must never run.
- Control-plane middleware exempts the bridge prefix **only when a bridge is mounted** (two principals, two credentials: operator `DERPR_CONTROL_TOKEN` vs per-dispatch bridge token) — not a standing hole on instances without one. DP-343 adds a **third** principal on the same pattern: the pve node's job-completion ping at `MODEL_JOB_CALLBACK_PATH`, holding `MODEL_JOB_CALLBACK_TOKEN`, exempted only while a `job_completion` handler is wired and checked constant-time inside the route. The two tokens are not interchangeable in either direction — an operator token is refused there, and the node token opens nothing else.
- `self_edit/dispatcher.py`: `dispatch(..., capable=False)`; capable mints a token, adds `--mcp-config` (http transport, token in the **header** not the URL) and appends the bridge host to sandbox `allowedDomains`. Capable-without-a-bridge is refused **before** `create_worktree` (a later refusal stranded an orphan worktree that nothing reaps — prune only walks registry rows). `answer_agent` reuses the SAME token (`token_for`) so a resumed agent keeps its tools; tokens revoked on DONE/ERROR/kill but **not** on QUESTION (a waiting agent is resumable).
- Config: `MCP_BRIDGE_ENABLED` (off), `MCP_BRIDGE_TOOLS` (**default-deny**), `MCP_BRIDGE_PATH`, `MCP_BRIDGE_PUBLIC_URL` (subagent's view; host auto-added to the sandbox allow-list). Wired in `main.py` §7.6 — the `AgentCallRunner` is built FIRST and shared with the proposal executor, so exposure and approval can't drift apart. Bridge-on + Zammad-off logs a warning: the review surface is unregistered, so gated calls would queue unapprovable.

### `src/proposals/` -- durable proposal queue (DP-282, managr Phase 1; self-managing queue DP-290)
Human-approval gating primitive for autonomous-agent writes — a **separate implementation** from `ConfirmationManager` (ADR 2026-07-04). ⚠️ The old justification — "that one is in-memory, one-pending-per-(user,persona), chat-turn-bound" — is **dead**: DP-297 made `ConfirmationManager` token-keyed and N-per-conversation, and DP-319 made it durable. What still separates them is `ProposalExecutor`'s `action_type` whitelist (arbitrary tool calls must not get a row it dispatches), nothing else. `ManagrAgent` proposes; a human approves via the portal; a separate executor writes. Documented in full in `docs/user_guide.md`.
- `schemas.py` — `PROPOSAL_ACTIONS` whitelist (`add_note` internal-only / `set_priority` / `remind`) + `validate_proposal_args` (required/type/enum/max_length/date, rejects unknown actions and unexpected keys). `build_submission_tool_schema()` derives the agent-internal `submit_proposals` tool from the whitelist.
- `executor.py` — `ProposalExecutor(zammad_client).execute(proposal) -> (bool, str)`. Re-validates args at execution time, resolves `ticket_number` → internal id via search, dispatches to the relevant Zammad write (add note / update priority / update state+pending_time). Errors returned, not raised.
- `service.py` — `ProposalIntegration(ServiceIntegration)`, `name="proposals"`; registers `list_proposals` (read), `approve_proposal`/`deny_proposal` (`is_write:True` → write gate). Approve reviews the row, audits, executes immediately, marks executed. Deny requires a reason. Lazy `expire_stale_proposals()` sweep on list/approve/deny. Registered in `main.py` only when Zammad is configured.
- Store: `Proposals` table in `memory_manager.py` — status pending/approved/denied/expired/executed/execution_failed/withdrawn; partial UNIQUE index on (agent, action_type, ticket_number) WHERE pending enforces dedup.
- Self-managing queue (DP-290): same-key pending proposal is UPSERTed in place instead of duplicated; `ManagrAgent` can also `reaffirm`/`revise`/`withdraw` its own pending proposals each cycle (reflective dispositions) — TTL acts as a GC backstop, not the primary lifecycle.
- Emission (in `ManagrAgent`): config-gated `proposals_enabled`; a second planner call with `tools=[submit_proposals]`; every candidate code-validated before insert, capped at `MANAGR_MAX_PROPOSALS_PER_CYCLE`.
- Standing orders (DP-281): the `Standing_Orders` table (`add`/`list`/`retire_standing_order`) holds operator-authored persistent instructions. Tools live on `StandingOrderToolHandler` (no executor dep) in `service.py`, same `proposals` binding; add/retire are audit-logged. `ManagrAgent` fetches orders **once per cycle** in `deploy()` (plan + extraction share the set), skips non-operator-source rows at injection, and on store failure prepends `STANDING_ORDERS_DEGRADED_NOTICE` to the digest + sets `standing_orders_degraded` in the outcome (fail-open is operator-visible). `MANAGR_STANDING_ORDERS_LIMIT` (20); `list_standing_orders` raises on limit<1 (SQLite `LIMIT -1` is unbounded) and caps at 200.
- **Subagent tool calls (DP-240):** `AGENT_CALL_ACTIONS` (`call_derpr_tool`: tool_name/tool_args/agent_id) is a **separate dict** from `PROPOSAL_ACTIONS`, and `EXECUTABLE_ACTIONS` is the union. Reason: `build_submission_tool_schema` enum-pins to `PROPOSAL_ACTIONS`, and managr reads attacker-reachable ticket text — a generic tool-call action in that dict would let a poisoned ticket steer managr into proposing arbitrary derpr calls. `validate_proposal_args(scope=...)` defaults to `PROPOSAL_ACTIONS` so every pre-existing caller stays fail-closed; only `ProposalExecutor` passes `EXECUTABLE_ACTIONS`.
- `agent_call.py` — `AgentCallRunner(tool_manager_lookup, policy_lookup)` (closures, not bound refs: the engine rebinds `tool_manager`). `exposed_tool_definitions()` = registered-handlers ∩ bridge `ToolPolicy` (one source for both `tools/list` and the execute-time check, so listing and execution can't disagree). `run()` **re-derives exposure at execution time** and refuses a stale row — the property that makes an indefinitely-queued approval safe. `execute_ungated()` is the immediate path for read tools. `executor.py` gains an optional `agent_call_runner`: `call_derpr_tool` short-circuits before ticket resolution; a `None` runner refuses loudly, never silently "executed".

### `src/main.py` -- Startup Sequence
1. MemoryManager (SQLite) + schema migration
2. TextEngine (LLM API router)
3. ZammadClient (optional, fails gracefully if no credentials)
4. EmbeddingService (GeminiEmbeddingProvider) — shared by ChatSystem and SqliteConsolidator
5. `bootstrap.create_chat_system(memory_manager, text_engine, embedding_service)` — loads personas, builds the ToolManager with core handlers, registers credentials into the scrubber, rebuilds parked writes from the durable store (DP-319), and constructs ChatSystem with real deps (DP-200)
6. Register ZammadIntegration service (if Zammad available)
7. AgentManager + register agent classes (`SqliteConsolidator` only when `SEMANTIC_BACKEND=="sqlite"`; `ZammadBot` + `DispatchAgent` + `ReminderAgent` + `ManagrAgent` only if Zammad available). Single-shot **inference** agents are registered here too via `register_inference_agent` — `date_tagger` (DP-292) and `content_classifier` (DP-294) — built by DI on first lookup, never launched as tasks
8. Register AgentServiceIntegration service (agent tools)
9. AppManager + NotificationRouter (Discord/Zammad notifiers; `discord_channel` = `DiscordChannelNotifier`)
9.1 Register FixrIntegration service (fixr dispatch tools, DP-227 — needs ChatSystem + NotificationRouter) [main.py step 7.1]
9.2 Register VoiceIntegration service (voice timer tools, DP-238 — needs NotificationRouter; Discord/web capture late-bound after the interfaces exist) [main.py step 7.2]
9.3 Register ProxmoxIntegration service (proxmox management tools, DP-262 — registration-only, always registers) [main.py step 7.3]
9.3b Register HuggingFaceIntegration service (HuggingFace model provisioning, DP-265 — always registers; shares the proxmox SSH transport). DP-343: constructed with the ChatSystem + NotificationRouter, and its `completion_bridge.handle` is passed to the engine adapter in step 8 as the node-job completion route's handler [main.py step 7.3b]
9.4 Register MCPIntegration service (MCP management tools, DP-268 — registration-only, always registers; constructs the main.py-owned `MCPClientManager` with `personas_provider`) [main.py step 7.4]; later `await mcp_manager.start()` connects configured servers and registers their discovered tools (no-op when `MCP_ENABLED` is false), and `mcp_manager.aclose()` runs at shutdown [main.py step 8.2]
9.5 Register ProposalIntegration service (proposal-queue review tools, DP-282 — only if Zammad available; wraps `ProposalExecutor(zammad_client)`) [main.py step 7.5]
9.6 MCP bridge (DP-240) — the `AgentCallRunner` is built FIRST and shared with the proposal executor so exposure and approval can't drift apart; the bridge is then mounted on the engine-adapter app [main.py step 7.6]
10. Register interface tasks (Discord bot, Gmail bot, `kobold_engine_api`); `fixr_orphan_notify` one-shot
10.1 `await bot.startup()` — post-init tasks, e.g. Hindsight bank provisioning [main.py step 8.1]
10.2 `await mcp_manager.start()` — connect configured MCP servers and register their discovered tools (no-op when `MCP_ENABLED` is false); `mcp_manager.aclose()` runs at shutdown [main.py step 8.2]
11. Register MemoryConsolidator as hourly background daemon — `app.register_task("memory_consolidator", consolidator.start_daemon(check_interval_seconds=3600), stop=consolidator.stop, drain_timeout=MemoryConsolidator.DRAIN_TIMEOUT_SECONDS)`. The `stop=` is what makes it a *drainer* rather than a task cancelled mid-cluster (DP-304)
12. Optional model list refresh on startup
13. `_install_shutdown_handlers(app)` → SIGINT/SIGTERM route to `app.request_shutdown()` (DP-304)
14. `app.start()` → auto_start agents + launch interface tasks

## Config

### `config/global_config.py`
- DEFAULT_HISTORY_MESSAGES = 15 (messages fetched from DB)
- GLOBAL_HISTORY_MESSAGES = 30 (hard cap when history_limit passed)
- MAX_TOOL_CALLS = 15 (tool calls executed per request. DP-297 raised it 5 → 10 because parking no longer ends the turn; **DP-335 moved the counter from iterations to calls** — the old number bought 10 calls on a serial model and 50 on a batching one — and sized 15 against hypr's 6-call provisioning floor plus two dead ends)
- MAX_TOOL_ITERATIONS = 25 (DP-335; pure runaway guard on LLM round trips, independent of how many calls each carries)
- MAX_CACHED_API_REQUESTS = 128
- DEFAULT_TOKEN_LIMIT = 4096 (LLM output)
- PENDING_ACTION_TTL = 86400 (seconds; DP-297 renamed + raised it from PENDING_CONFIRMATION_TIMEOUT = 300, since a park became a queue worked through later rather than a blocking modal. DP-319 made the store durable (`Parked_Writes`), so this is now the real deadline rather than min(this, process uptime))
- PARK_ROW_RETENTION / PARK_PURGE_INTERVAL / PARK_REEXECUTION_GUARD_WINDOW (DP-319) — how long a terminal park row is kept for the duplicate guard, how often the lazy sweep purges past-retention rows, and the window in which a re-proposed write is answered "already resolved" instead of parked again
- EMPTY_RESPONSE_RETRIES = 3, EMPTY_RESPONSE_RETRY_DELAY = 2
- DISCORD_CHAR_LIMIT, DISCORD_STATUS_LIMIT, DISCORD_DEBUG_CHANNEL, AMBIENT_LOGGING_CHANNELS
- Rate limits per model family (RPM/RPD): GEMINI_25, GEMINI_3, GEMMA_3, GEMMA_4 (also RATE_LIMIT_GEMMA_4_TPR = 256000 tokens per request), OPENAI, ANTHROPIC
- Long-term memory: MEMORY_RETRIEVAL_ENABLED = True, MEMORY_MAX_SUMMARIES_IN_CONTEXT = 5
- Embedding: EMBEDDING_MODEL = 'gemini-embedding-001', EMBEDDING_DIMENSION = 3072
- Embedding rate limits: GEMINI_EMBEDDING_001_RPM = 100, GEMINI_EMBEDDING_001_TPM = 30000, GEMINI_EMBEDDING_001_RPD = 1000

### Persona Config Files
- `config/default_personas.json` -- tracked in git, seeds production
- `config/system_personas.json` -- tracked in git, system personas (used by agents for read-only LLM analysis)
- `data/personas.json` -- gitignored, local runtime state, overrides defaults on startup

### Agent Config
- `config/agents.json` -- agent definitions, schedule, auto_start, notification_defaults, recipient mappings
- Structure: `{agents: {name: {persona, schedule, action_history_limit, auto_start, notification_defaults}}, recipients: {name: {discord_user_id, email}}}`

## Testing Structure

4-tier, ordered by execution:
1. **Unit** (no marker) -- everything mocked, no network
2. **Integration** (`@pytest.mark.integration`) -- multi-component with mocked externals
3. **Zammad Live** (`@pytest.mark.zammad_live`) -- requires live Zammad instance
4. **LLM Live** (`@pytest.mark.llm_live`) -- real LLM API calls

Key test files:
- `tests/test_chat_system.py` -- ChatSystem with mocked deps, `chat_system_with_mocks` fixture
- `tests/test_engine.py` -- TextEngine provider tests
- `tests/memory/test_memory_manager.py` -- schema, CRUD, migration tests (`legacy_mem_manager` fixture)
- `tests/memory/test_memory_consolidation.py` -- consolidation clustering and merge tests
- `tests/agents/test_memory_agent.py` -- segmentation, summarization, embedding, deploy cycle
- `tests/test_memory_retrieval.py` -- retrieval relevance, model filtering, level filtering
- `tests/test_embedding_service.py` -- embedding provider, chunking, rate limiting
- `tests/unit/test_memory_glue.py` -- semantic glue (Q/A pair preservation in segmenter)
- `tests/integration/test_full_system_flow.py` -- end-to-end with mocked externals
- `tests/integration/test_memory_modes.py` -- memory mode behavior
- `tests/interfaces/test_discord_bot.py` -- Discord bot event handling
- `tests/interfaces/test_gmail_bot.py` -- Gmail bot message handling
- `tests/test_module_boundaries.py` -- **architecture enforcement** (DP-200): AST-walks `src/` and asserts the layer import rules (CONTRACTS); grandfathered violations live in KNOWN_DEBT, which fails when stale so it can only shrink. Update it whenever modules move.
- `tests/integration/test_startup_wiring.py` -- asserts every tool `service_binding` in `ALL_TOOL_DEFINITIONS` has a registered handler; update when adding a new service
- `tests/test_engine_payload_parity.py` -- wire-payload goldens pinning the exact request kwargs per provider (frozen dicts; only capture points may move)

Migration test pattern: `legacy_mem_manager` fixture creates DB with OLD schema (no new columns/tables), test calls `create_schema()` and verifies migration.
